<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>使用github pages和hexo部署个人博客网站</title>
    <url>/2022/04/01/Site/hexo-github-pages/</url>
    <content><![CDATA[<h1 id="使用-github-pages-和-hexo-部署个人博客网站"><a href="#使用-github-pages-和-hexo-部署个人博客网站" class="headerlink" title="使用 github pages 和 hexo 部署个人博客网站"></a>使用 github pages 和 hexo 部署个人博客网站</h1><p>在本篇博客中，我将详细介绍如何从零开始搭建个人博客网站，使用 Hexo 创建博客网站，并将其部署到 GitHub Pages，并最终绑定自定义域名。让我们一起来跟随下面的步骤进行：</p>
<h2 id="步骤一：使用-Hexo-创建博客网站"><a href="#步骤一：使用-Hexo-创建博客网站" class="headerlink" title="步骤一：使用 Hexo 创建博客网站"></a>步骤一：使用 Hexo 创建博客网站</h2><ol>
<li>安装 Node.js 和 Git：<br>首先确保您的电脑上安装了 Node.js 和 Git。打开命令行工具，运行以下命令安装 Hexo：</li>
</ol>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">npm install -g hexo-cli</span><br></pre></td></tr></table></figure>

<ol start="2">
<li>初始化项目：<br>创建一个新的 Hexo 项目，并进入项目目录：</li>
</ol>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">hexo init my-blog</span><br><span class="line">cd my-blog</span><br></pre></td></tr></table></figure>

<ol start="3">
<li>添加新文章：<br>创建新的文章，使用 Markdown 格式编写文章内容：</li>
</ol>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">hexo new &quot;My First Post&quot;</span><br></pre></td></tr></table></figure>

<h2 id="步骤二：部署到-GitHub-Pages"><a href="#步骤二：部署到-GitHub-Pages" class="headerlink" title="步骤二：部署到 GitHub Pages"></a>步骤二：部署到 GitHub Pages</h2><ol>
<li><p>创建 GitHub 仓库：<br>在 GitHub 上创建一个新的仓库，用于存储博客网站的代码。</p>
</li>
<li><p>编辑配置文件：<br>打开 _config.yml 文件，修改 deploy 配置为 GitHub Pages：</p>
</li>
</ol>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">deploy:</span><br><span class="line">type: git</span><br><span class="line">repo: https://github.com/your-username/your-repo.git</span><br><span class="line">branch: gh-pages</span><br></pre></td></tr></table></figure>

<ol start="3">
<li>安装 hexo-deployer-git：<br>根目录下执行如下命令：</li>
</ol>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">npm install hexo-deployer-git --save</span><br></pre></td></tr></table></figure>

<p>之后就可以让 hexo 使用 git 来部署页面到 github 仓库中</p>
<ol start="4">
<li>部署网站：<br>运行以下命令部署网站到 GitHub Pages：</li>
</ol>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">hexo deploy</span><br></pre></td></tr></table></figure>

<h2 id="步骤三：绑定自定义域名"><a href="#步骤三：绑定自定义域名" class="headerlink" title="步骤三：绑定自定义域名"></a>步骤三：绑定自定义域名</h2><ol start="7">
<li><p>配置 CNAME 文件：<br>在博客项目目录中创建 CNAME 文件，输入您的自定义域名 domain.com。</p>
</li>
<li><p>绑定域名：<br>进入 GitHub 仓库设置，将您的自定义域名 domain.com 添加到仓库的 Custom Domain，点击 Save 后，github 会自动开始检查域名是否可用。</p>
</li>
<li><p>配置 DNS 记录：<br>在您的 DNS 控制面板中，添加一条 CNAME 记录，将域名指向 GitHub Pages 提供的域名 your-username.github.io.</p>
</li>
</ol>
<p>通过以上步骤，您将成功从零开始搭建个人博客网站，使用 Hexo 创建博客，并将其部署到 GitHub Pages，并绑定自定义域名。祝您的博客网站一切顺利！如有任何问题或需要帮助，请随时告诉我。希朿您顺利搭建个人博客网站！</p>
]]></content>
      <categories>
        <category>Site</category>
      </categories>
      <tags>
        <tag>Hexo</tag>
        <tag>GitHub Pages</tag>
        <tag>博客</tag>
        <tag>网站</tag>
      </tags>
  </entry>
  <entry>
    <title>使用 next-on-pages 部署 nextjs 项目时遇到的一些问题</title>
    <url>/2024/01/23/Site/issues-about-next-on-pages/</url>
    <content><![CDATA[<h1 id="使用-next-on-pages-部署-nextjs-项目时遇到的一些问题"><a href="#使用-next-on-pages-部署-nextjs-项目时遇到的一些问题" class="headerlink" title="使用 next-on-pages 部署 nextjs 项目时遇到的一些问题"></a>使用 next-on-pages 部署 nextjs 项目时遇到的一些问题</h1><p>使用 next-on-pages 将 nextjs 项目部署到 cloudflare 上时遇到的一些问题</p>
<h2 id="创建一个-next-on-pages-项目"><a href="#创建一个-next-on-pages-项目" class="headerlink" title="创建一个 next-on-pages 项目"></a>创建一个 next-on-pages 项目</h2><p>对于已有的 nextjs 项目, 可以在项目中添加 @cloudflare&#x2F;next-on-pages 依赖:</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">npm i -D @cloudflare/next-on-pages</span><br></pre></td></tr></table></figure>

<p>并且在构建时需要修改 scripts:</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;scripts&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;build&quot;</span><span class="punctuation">:</span> <span class="string">&quot;npx @cloudflare/next-on-pages@1&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="comment">// or</span></span><br><span class="line">    <span class="attr">&quot;build&quot;</span><span class="punctuation">:</span> <span class="string">&quot;npx next-on-pages&quot;</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p>部署到 cloudflare pages 上可以使用 wrangler 来进行部署</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">npm i -D wrangler</span><br></pre></td></tr></table></figure>

<p>部署命令:</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;scripts&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;deploy&quot;</span><span class="punctuation">:</span> <span class="string">&quot;npm run build &amp;&amp; wrangler pages deploy&quot;</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p>对于新建的项目, 则推荐直接使用 wrangler 来创建 nextjs 项目</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">npm create cloudflare@latest my-pages</span><br><span class="line"><span class="comment"># 根据后续提示选择nextjs框架</span></span><br></pre></td></tr></table></figure>

<p>脚手架会创建一个基于 create-next-app 默认模板的 nextjs 项目, 随后开发流程和 nextjs 一样</p>
<h2 id="Windows-系统构建报错"><a href="#Windows-系统构建报错" class="headerlink" title="Windows 系统构建报错"></a>Windows 系统构建报错</h2><p>在 windows 上用 next-on-pages 构建 nextjs 项目时有时会出错, 通常抛出的是 nodejs 的 child_process 执行命令时的错误, 没有明确的错误说明</p>
<p>但在使用 next-on-pages 也会提示在 windows 上不稳定:</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">⚡️ Warning: It seems like you<span class="string">&#x27;re on a Windows system, the Vercel CLI (run by @cloudflare/next-on-pages</span></span><br><span class="line"><span class="string">⚡️ to build your application) seems not to work reliably on Windows so if you experience issues during</span></span><br><span class="line"><span class="string">⚡️ the build process please try switching to a different operating system or running</span></span><br><span class="line"><span class="string">⚡️ @cloudflare/next-on-pages under the Windows Subsystem for Linux</span></span><br></pre></td></tr></table></figure>

<p>所以可以将项目迁移到 linux 系统或 WSL 中来构建, 同时也需要将 npm 等基本环境安装到 WSL 中</p>
<p>如果不想安装 WSL, 也可以将项目上传到 github 然后连接到 cloudflare pages 空间, 利用云服务器来进行构建</p>
<h2 id="如何在-cloudflare-上连接-github-仓库"><a href="#如何在-cloudflare-上连接-github-仓库" class="headerlink" title="如何在 cloudflare 上连接 github 仓库"></a>如何在 cloudflare 上连接 github 仓库</h2><ol>
<li>进入 cloudflare dashboard, 打开 Workers &amp; Pages 的 Overview 页面, 点击 Create</li>
</ol>
<p><img src="/images/issues-about-next-on-pages-01.png" alt="issues-about-next-on-pages-01"></p>
<ol start="2">
<li>选择 Pages 并点击 Connect to Git</li>
</ol>
<p><img src="/images/issues-about-next-on-pages-02.png" alt="issues-about-next-on-pages-02"></p>
<p>cloudflare 目前支持 github 和 gitlab 自动部署, 如果之前已经在 cloudflare 连接过 github 账号, 此时就已经能看到自己的 github 账号下的所有公开仓库了, 选择想要部署的项目然后点击 Begin Setup</p>
<ol start="3">
<li>进入项目构建配置页面, 选择项目分支, 预设的开发框架, 输入构建命令和输出静态文件的目录(相对于根目录), 还可以选择添加环境变量或者改变根目录所在位置</li>
</ol>
<p>当选择 nextjs 作为开发框架时, cloudflare 通常会自动配置好命令和输出目录</p>
<p><img src="/images/issues-about-next-on-pages-03.png" alt="issues-about-next-on-pages-03"></p>
<p>都配置好以后点击 Save and Deploy 就会开始自动构建和部署</p>
<h2 id="构建-nextjs-之前需要注意的细节"><a href="#构建-nextjs-之前需要注意的细节" class="headerlink" title="构建 nextjs 之前需要注意的细节"></a>构建 nextjs 之前需要注意的细节</h2><p>需要注意的是, 在 cloudflare 上部署 nextjs 项目时, 项目中使用的 api 路由需要额外配置运行时为 edge</p>
<p>项目中 &#x2F;api 下的所有 route.ts 中均需要加上如下代码:</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="keyword">const</span> runtime = <span class="string">&quot;edge&quot;</span>;</span><br></pre></td></tr></table></figure>

<p>同时 nextjs 构建时也会强制执行 ts 类型检查, 需要注意项目中的 route.ts, layout.ts, page.ts 等文件中不要导出无关的代码</p>
]]></content>
      <categories>
        <category>Site</category>
      </categories>
      <tags>
        <tag>next-on-pages</tag>
        <tag>nextjs</tag>
        <tag>cloudflare</tag>
        <tag>wrangler</tag>
      </tags>
  </entry>
  <entry>
    <title>一些关于hexo网站的小技巧</title>
    <url>/2022/04/01/Site/tricks-about-hexo/</url>
    <content><![CDATA[<h1 id="一些关于-hexo-网站的小技巧"><a href="#一些关于-hexo-网站的小技巧" class="headerlink" title="一些关于 hexo 网站的小技巧"></a>一些关于 hexo 网站的小技巧</h1><p>一些在使用 hexo 建站时有用的小技巧</p>
<h2 id="自动创建-post"><a href="#自动创建-post" class="headerlink" title="自动创建 post"></a>自动创建 post</h2><p>项目根目录执行以下命令:</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">hexo new post my-blog</span><br></pre></td></tr></table></figure>

<p>执行后就会在 &#x2F;source&#x2F;_post 下新增一个 my-blog.md 文件, 并且会自动生成简单的 frontmatter 信息</p>
<p>若想在_post 的子目录中创建文章, 则需要带上 -p 参数</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">hexo new post -p blogs/my-blog</span><br></pre></td></tr></table></figure>

<p>这样则会在 &#x2F;source&#x2F;_post&#x2F;blogs 目录下创建文件</p>
<h2 id="显示菜单项"><a href="#显示菜单项" class="headerlink" title="显示菜单项"></a>显示菜单项</h2><p>打开根目录下的_config.yml, 或你使用的主题所在的目录下的_config.yml</p>
<p>找到 menu: 配置项, 如下:</p>
<figure class="highlight yml"><table><tr><td class="code"><pre><span class="line"><span class="comment"># Usage: `Key: /link/ || icon`</span></span><br><span class="line"><span class="comment"># Key is the name of menu item. If the translation for this item is available, the translated text will be loaded, otherwise the Key name will be used. Key is case-senstive.</span></span><br><span class="line"><span class="comment"># Value before `||` delimiter is the target link, value after `||` delimiter is the name of Font Awesome icon.</span></span><br><span class="line"><span class="comment"># When running the site in a subdirectory (e.g. yoursite.com/blog), remove the leading slash from link value (/archives -&gt; archives).</span></span><br><span class="line"><span class="comment"># External url should start with http:// or https://</span></span><br><span class="line"><span class="attr">menu:</span></span><br><span class="line">  <span class="attr">home:</span> <span class="string">/</span> <span class="string">||</span> <span class="string">fa</span> <span class="string">fa-home</span></span><br><span class="line">  <span class="comment">#about: /about/ || fa fa-user</span></span><br><span class="line">  <span class="comment">#tags: /tags/ || fa fa-tags</span></span><br><span class="line">  <span class="comment">#categories: /categories/ || fa fa-th</span></span><br><span class="line">  <span class="attr">archives:</span> <span class="string">/archives/</span> <span class="string">||</span> <span class="string">fa</span> <span class="string">fa-archive</span></span><br><span class="line">  <span class="comment">#schedule: /schedule/ || fa fa-calendar</span></span><br><span class="line">  <span class="comment">#sitemap: /sitemap.xml || fa fa-sitemap</span></span><br><span class="line">  <span class="comment">#commonweal: /404/ || fa fa-heartbeat</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Enable / Disable menu icons / item badges.</span></span><br><span class="line"><span class="attr">menu_settings:</span></span><br><span class="line">  <span class="attr">icons:</span> <span class="literal">true</span></span><br><span class="line">  <span class="attr">badges:</span> <span class="literal">false</span></span><br></pre></td></tr></table></figure>

<p>取消对应菜单项的注释, 重新运行 hexo, 就可以看到相应菜单显示出来</p>
<p>菜单项中每一个配置的规则为:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Usage: `Key: /link/ || icon`</span><br></pre></td></tr></table></figure>

<p>若配置 tags 菜单, 该菜单会显示配置的 icon 的图标, 而点击该菜单时会去寻找 public&#x2F;tags 下的 index.html 页面</p>
<p>hexo 默认会生成 home 和 archives 两个页面, 若需要显示其他页面, 则需要创建对应的 page, 如生成 tags 页面:</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">hexo new page tags <span class="comment"># 生成tags page</span></span><br></pre></td></tr></table></figure>

<p>此时在项目的 source&#x2F;tags 下会生成一个 index.md 文件, 打开该文件修改 frontmatter 如下:</p>
<figure class="highlight md"><table><tr><td class="code"><pre><span class="line">---</span><br><span class="line">title: tags</span><br><span class="line">date: 2020-01-01 00:00:00</span><br><span class="line">type: &quot;tags&quot;</span><br><span class="line"><span class="section">comments: false</span></span><br><span class="line"><span class="section">---</span></span><br></pre></td></tr></table></figure>

<p>再次运行 hexo, 点击 tags 菜单则会正确打开 tags 页面</p>
<h2 id="显示-github-图标"><a href="#显示-github-图标" class="headerlink" title="显示 github 图标"></a>显示 github 图标</h2><p>在_config.yml 中找到 github_banner 配置项, 设置 enable 为 true, 并添加自己的 github 链接:</p>
<figure class="highlight yml"><table><tr><td class="code"><pre><span class="line"><span class="comment"># `Follow me on GitHub` banner in the top-right corner.</span></span><br><span class="line"><span class="attr">github_banner:</span></span><br><span class="line">  <span class="attr">enable:</span> <span class="literal">true</span></span><br><span class="line">  <span class="attr">permalink:</span> <span class="string">https://github.com/your-name</span></span><br><span class="line">  <span class="attr">title:</span> <span class="string">Follow</span> <span class="string">me</span> <span class="string">on</span> <span class="string">GitHub</span></span><br></pre></td></tr></table></figure>

<h2 id="隐藏内容点击展开"><a href="#隐藏内容点击展开" class="headerlink" title="隐藏内容点击展开"></a>隐藏内容点击展开</h2><p>使用 html5 提供的 details 标签, 在 summary 标签中写入隐藏时显示的文字, 在其后的 p 标签中写入详细内容, 如:</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">details</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">summary</span>&gt;</span>标题<span class="tag">&lt;/<span class="name">summary</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">p</span>&gt;</span>详情<span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">details</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>展示效果如下:</p>
<details>
  <summary>标题</summary>
  <p>详情</p>
</details>

<h2 id="自动添加-github-pages-自定义域名"><a href="#自动添加-github-pages-自定义域名" class="headerlink" title="自动添加 github pages 自定义域名"></a>自动添加 github pages 自定义域名</h2><p>由于 github 的限制, 在使用自定义域名时, 每次提交代码后自定义域名都会失效, 需要重新配置</p>
<p>要解决这个问题, 只需要在 source 目录下添加一个 CNAME 文件, 并在其中写入你的域名即可, 如:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">yourdomain.net</span><br></pre></td></tr></table></figure>

<p>随后使用 hexo g 提交到 GitHub 上就会自动在 pages 中设置该自定义域名</p>
<h2 id="使用搜索功能"><a href="#使用搜索功能" class="headerlink" title="使用搜索功能"></a>使用搜索功能</h2><p>安装相关插件:</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">npm i -S hexo-generator-searchdb</span><br></pre></td></tr></table></figure>

<p>在 _config.yml 配置文件中添加如下配置:</p>
<figure class="highlight yml"><table><tr><td class="code"><pre><span class="line"><span class="attr">search:</span></span><br><span class="line">  <span class="attr">path:</span> <span class="string">search.xml</span></span><br><span class="line">  <span class="attr">field:</span> <span class="string">post</span></span><br><span class="line">  <span class="attr">format:</span> <span class="string">html</span></span><br><span class="line">  <span class="attr">limit:</span> <span class="number">10000</span></span><br></pre></td></tr></table></figure>

<p>使用 Next 主题时自带本地搜索设置, 编辑主题中的 _config.yml 文件中的 local_search:</p>
<figure class="highlight yml"><table><tr><td class="code"><pre><span class="line"><span class="comment"># Local_search</span></span><br><span class="line"></span><br><span class="line"><span class="attr">local_search:</span></span><br><span class="line">  <span class="attr">eanble:</span> <span class="literal">true</span></span><br></pre></td></tr></table></figure>

<h2 id="侧边栏社交链接"><a href="#侧边栏社交链接" class="headerlink" title="侧边栏社交链接"></a>侧边栏社交链接</h2><p>侧边栏社交链接的配置有两部分, 一个是链接, 一个是连接图标, 在使用主题的 _config.yml 中配置</p>
<p>链接在 social 配置项下面, 格式为 <code>显示文本: 链接地址 || 显示的 Font Awesome 图标</code></p>
<figure class="highlight yml"><table><tr><td class="code"><pre><span class="line"><span class="attr">social:</span></span><br><span class="line">  <span class="attr">GitHub:</span> <span class="string">https://github.com/yourname</span> <span class="string">||</span> <span class="string">fab</span> <span class="string">fa-github</span></span><br><span class="line">  <span class="comment">#E-Mail: mailto:yourname@gmail.com || fa fa-envelope</span></span><br><span class="line">  <span class="comment">#Weibo: https://weibo.com/yourname || fab fa-weibo</span></span><br><span class="line">  <span class="comment">#Google: https://plus.google.com/yourname || fab fa-google</span></span><br><span class="line">  <span class="comment">#Twitter: https://twitter.com/yourname || fab fa-twitter</span></span><br><span class="line">  <span class="comment">#FB Page: https://www.facebook.com/yourname || fab fa-facebook</span></span><br><span class="line">  <span class="comment">#StackOverflow: https://stackoverflow.com/yourname || fab fa-stack-overflow</span></span><br><span class="line">  <span class="comment">#YouTube: https://youtube.com/yourname || fab fa-youtube</span></span><br><span class="line">  <span class="comment">#Instagram: https://instagram.com/yourname || fab fa-instagram</span></span><br><span class="line">  <span class="comment">#Skype: skype:yourname?call|chat || fab fa-skype</span></span><br></pre></td></tr></table></figure>

<p>图标的设置在 social_icons 配置项下面, 可以开启是否显示图标</p>
<figure class="highlight yml"><table><tr><td class="code"><pre><span class="line"><span class="attr">social_icons:</span></span><br><span class="line">  <span class="attr">enable:</span> <span class="literal">true</span></span><br><span class="line">  <span class="attr">icons_only:</span> <span class="literal">false</span></span><br><span class="line">  <span class="attr">transition:</span> <span class="literal">false</span></span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>Site</category>
      </categories>
      <tags>
        <tag>Hexo</tag>
        <tag>博客</tag>
        <tag>技巧</tag>
      </tags>
  </entry>
  <entry>
    <title>用typescript实现依赖注入模式</title>
    <url>/2022/04/03/Coding/dependency-injection-ts/</url>
    <content><![CDATA[<h1 id="用-typescript-实现依赖注入模式"><a href="#用-typescript-实现依赖注入模式" class="headerlink" title="用 typescript 实现依赖注入模式"></a>用 typescript 实现依赖注入模式</h1><p>本篇文章将介绍如何使用 typescript 来实现依赖注入功能，并考虑到各种情况，包括实现单例和处理循环依赖等</p>
<h2 id="概念"><a href="#概念" class="headerlink" title="概念"></a>概念</h2><p>依赖注入的概念非常简单。它基于一个容器（Container），用于管理对象的创建和解决依赖关系。在使用依赖注入之前，我们需要定义一些“可注入”的类（Injectable），并使用“Inject”标记它们的依赖关系。现在，让我们来逐步实现这个过程。</p>
<h2 id="实现"><a href="#实现" class="headerlink" title="实现"></a>实现</h2><h3 id="Step-1-创建-injectable-装饰器"><a href="#Step-1-创建-injectable-装饰器" class="headerlink" title="Step.1: 创建 @injectable 装饰器"></a>Step.1: 创建 @injectable 装饰器</h3><p>@injectable 可以将一个类标记为一个可注入类, 可以使用 reflect-metadata 库来实现这个装饰器</p>
<p>首先, 安装 reflect-metadata 库</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">npm i reflect-metadata</span><br></pre></td></tr></table></figure>

<p>之后在引入这个库并定义 injectable 函数</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> <span class="string">&quot;reflect-metadata&quot;</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">function</span> <span class="title function_">Injectable</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">function</span> (<span class="params"><span class="attr">taget</span>: <span class="built_in">any</span></span>) &#123;</span><br><span class="line">    <span class="title class_">Reflect</span>.<span class="title function_">defineMetadata</span>(<span class="string">&quot;injectable&quot;</span>, <span class="literal">true</span>, target);</span><br><span class="line">  &#125;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这个装饰器使用 Reflect.defineMetadata 为被装饰的类添加了一个 injectable 的元数据标识</p>
<h3 id="step-2-创建-inject-装饰器"><a href="#step-2-创建-inject-装饰器" class="headerlink" title="step.2: 创建 @inject 装饰器"></a>step.2: 创建 @inject 装饰器</h3><p>@inject 装饰器用于标记类的依赖关系</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> <span class="string">&quot;reflect-metadata&quot;</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">function</span> <span class="title function_">Inject</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">function</span> (<span class="params"><span class="attr">target</span>: <span class="built_in">any</span>, <span class="attr">propertyKey</span>: <span class="built_in">string</span></span>) &#123;</span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">type</span> = <span class="title class_">Reflect</span>.<span class="title function_">getMetadata</span>(<span class="string">&quot;design:type&quot;</span>, target, propertyKey);</span><br><span class="line">    <span class="title class_">Reflect</span>.<span class="title function_">defineMetadata</span>(<span class="string">&quot;inject&quot;</span>, <span class="keyword">type</span>, target, propertyKey);</span><br><span class="line">  &#125;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这个装饰器使用 Reflect.getMetadata 获取标记属性的类型, 随后用 Reflect.defineMetadata 将类型信息储存到元数据中</p>
<h3 id="step-3-实现容器类"><a href="#step-3-实现容器类" class="headerlink" title="step.3: 实现容器类"></a>step.3: 实现容器类</h3><p>容器类 Container 用于管理类的实例的创建和解决依赖关系, 需要支持以下功能:</p>
<ol>
<li>注册标记为 @injectable 的类</li>
<li>处理依赖关系</li>
<li>创建单例</li>
<li>解决循环依赖</li>
</ol>
<figure class="highlight ts"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> <span class="string">&quot;reflect-metadata&quot;</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Container</span> &#123;</span><br><span class="line">  <span class="keyword">private</span> <span class="attr">instances</span>: <span class="title class_">Map</span>&lt;<span class="title class_">Constructor</span>, <span class="built_in">any</span>&gt; = <span class="keyword">new</span> <span class="title class_">Map</span>(); <span class="comment">// 单例</span></span><br><span class="line">  <span class="keyword">private</span> <span class="attr">resolvingQueue</span>: <span class="title class_">Set</span>&lt;<span class="title class_">Constructor</span>&gt; = <span class="keyword">new</span> <span class="title class_">Set</span>(); <span class="comment">// 解析队列</span></span><br><span class="line"></span><br><span class="line">  register&lt;T&gt;(<span class="attr">cls</span>: <span class="title class_">Constructor</span>&lt;T&gt;) &#123;</span><br><span class="line">    <span class="keyword">const</span> injectable = <span class="title class_">Reflect</span>.<span class="title function_">getMetadata</span>(<span class="string">&quot;injectable&quot;</span>, cls);</span><br><span class="line">    <span class="keyword">if</span> (!injectable) &#123;</span><br><span class="line">      <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">Error</span>(<span class="string">`Class <span class="subst">$&#123;cls.name&#125;</span> is not marked with @injectable`</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="variable language_">this</span>.<span class="property">instances</span>.<span class="title function_">set</span>(cls, <span class="literal">null</span>);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  resolve&lt;T&gt;(<span class="attr">cls</span>: <span class="title class_">Constructor</span>&lt;T&gt;): T &#123;</span><br><span class="line">    <span class="keyword">if</span> (<span class="variable language_">this</span>.<span class="property">resolvingQueue</span>.<span class="title function_">has</span>(cls)) &#123;</span><br><span class="line">      <span class="comment">// 当解析队列中包含这个类时, 表示出现循环依赖, 抛出错误</span></span><br><span class="line">      <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">Error</span>(<span class="string">`Circular dependency detected for class <span class="subst">$&#123;cls.name&#125;</span>`</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">const</span> instance = <span class="variable language_">this</span>.<span class="property">instances</span>.<span class="title function_">get</span>(cls);</span><br><span class="line">    <span class="keyword">if</span> (instance !== <span class="literal">null</span>) &#123;</span><br><span class="line">      <span class="comment">// 当存在已解析的实例时, 则返回该实例, 实现单例模式</span></span><br><span class="line">      <span class="keyword">return</span> instance;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="variable language_">this</span>.<span class="property">resolvingQueue</span>.<span class="title function_">add</span>(cls);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">const</span> injectParams = <span class="title class_">Reflect</span>.<span class="title function_">getMetadata</span>(<span class="string">&quot;design:paramtypes&quot;</span>, cls) || []; <span class="comment">// 获取构造函数的参数类型列表</span></span><br><span class="line">    <span class="keyword">const</span> resolvedParams = injectParams.<span class="title function_">map</span>(<span class="function">(<span class="params">param</span>) =&gt;</span> <span class="variable language_">this</span>.<span class="title function_">resolve</span>(param)); <span class="comment">// 递归获取参数类的单例</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">const</span> newInstance = <span class="keyword">new</span> <span class="title function_">cls</span>(...resolvedParams);</span><br><span class="line">    <span class="variable language_">this</span>.<span class="property">instances</span>.<span class="title function_">set</span>(cls, newInstance);</span><br><span class="line"></span><br><span class="line">    <span class="variable language_">this</span>.<span class="property">resolvingQueue</span>.<span class="title function_">delete</span>(cls);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> newInstance;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这个 Container 类有两个主要方法: register 用于注册可注入的类, resolve 用于处理依赖关系, 并且维护单例和检查循环依赖</p>
<h2 id="示例"><a href="#示例" class="headerlink" title="示例"></a>示例</h2><p>将我们的依赖注入模块作为一个库引入, 来写一个简单示例</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; <span class="title class_">Injectable</span>, <span class="title class_">Inject</span>, <span class="title class_">Container</span> &#125; <span class="keyword">from</span> <span class="string">&quot;my-di-lib&quot;</span>;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Injectable</span>()</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Foo</span> &#123;</span><br><span class="line">  <span class="title function_">constructor</span>(<span class="params"><span class="meta">@Inject</span>() <span class="keyword">public</span> <span class="attr">bar</span>: <span class="title class_">Bar</span></span>) &#123;&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Injectable</span>()</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Bar</span> &#123;&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> container = <span class="keyword">new</span> <span class="title class_">Container</span>();</span><br><span class="line"></span><br><span class="line">container.<span class="title function_">register</span>(<span class="title class_">Foo</span>);</span><br><span class="line">container.<span class="title function_">register</span>(<span class="title class_">Bar</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> foo = container.<span class="title function_">resolve</span>(<span class="title class_">Foo</span>);</span><br><span class="line">foo <span class="keyword">instanceof</span> <span class="title class_">Foo</span>; <span class="comment">// true</span></span><br><span class="line">foo.<span class="property">bar</span> <span class="keyword">instanceof</span> <span class="title class_">Bar</span>; <span class="comment">// true</span></span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>Coding</category>
      </categories>
      <tags>
        <tag>Typescript</tag>
        <tag>依赖注入</tag>
        <tag>单例模式</tag>
        <tag>设计模式</tag>
      </tags>
  </entry>
  <entry>
    <title>ES6 Proxy 对象特性和存在的缺陷</title>
    <url>/2022/04/24/Coding/flaws-of-js-proxy/</url>
    <content><![CDATA[<h1 id="ES6-Proxy-对象特性和存在的缺陷"><a href="#ES6-Proxy-对象特性和存在的缺陷" class="headerlink" title="ES6 Proxy 对象特性和存在的缺陷"></a>ES6 Proxy 对象特性和存在的缺陷</h1><p>详细介绍 ES6 中新加入的 Proxy 对象的特性和在使用中存在的缺陷</p>
<h2 id="Proxy-实例"><a href="#Proxy-实例" class="headerlink" title="Proxy 实例"></a>Proxy 实例</h2><p>首先看看 Proxy 是如何代理一个对象的</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Person</span> &#123;</span><br><span class="line">  <span class="title function_">constructor</span>(<span class="params">first, last</span>) &#123;</span><br><span class="line">    <span class="variable language_">this</span>.<span class="property">firstName</span> = first;</span><br><span class="line">    <span class="variable language_">this</span>.<span class="property">lastName</span> = last;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">get</span> <span class="title function_">fullName</span>() &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="string">`<span class="subst">$&#123;<span class="variable language_">this</span>.firstName&#125;</span> <span class="subst">$&#123;<span class="variable language_">this</span>.lastName&#125;</span>`</span>;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="title function_">introduce</span>(<span class="params">other = <span class="string">&quot;friend&quot;</span></span>) &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">`Hello <span class="subst">$&#123;other&#125;</span>, my name is <span class="subst">$&#123;<span class="variable language_">this</span>.fullName&#125;</span>!`</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> leo = <span class="keyword">new</span> <span class="title class_">Person</span>(<span class="string">&quot;Leo&quot;</span>, <span class="string">&quot;Messi&quot;</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> proxy = <span class="keyword">new</span> <span class="title class_">Proxy</span>(leo, &#123;</span><br><span class="line">  <span class="title function_">get</span>(<span class="params">target, prop</span>) &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">`Access: <span class="subst">$&#123;prop&#125;</span>`</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="title class_">Reflect</span>.<span class="title function_">get</span>(target, prop);</span><br><span class="line">  &#125;,</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line">proxy.<span class="title function_">introduce</span>();</span><br></pre></td></tr></table></figure>

<p>上面的代码实例化了一个 Proxy 对象, 传递一个 Person 对象来作为被代理的对象, 这时, 调用 proxy 的 get 方法要做两件事:</p>
<ol>
<li>将正在检索的对象键打印出来</li>
<li>使用 Reflect.get 将属性值从实例化的 Person 对象中取出来</li>
</ol>
<p>上面的打印结果是:</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">Access: introduce</span><br><span class="line">Access: fullName</span><br><span class="line">Hello friend, my name is Leo Messi!</span><br></pre></td></tr></table></figure>

<ol>
<li>在 js 的对象中,调用一个方法时, 必然先调用对象上的 get 方法, 所以第一条打印信息是 introduce</li>
<li>此时, introduce 方法中的 this 指向 proxy 对象, 所以调用 this.fullName 会再次调用 proxy 的 get 方法打印第二条信息</li>
<li>最后打印 introduce 方法的返回值</li>
</ol>
<p>但为什么没有打印 firstName 和 lastName 呢, 当访问 fullName 时内部确实也访问了这两个变量的</p>
<p>在我们使用 Reflect.get 访问内部的 fullName 时, 因为 fullName 是一个属性, 它会调用属性描述符上的 get 方法, 此时的 this 在运行时已经指向了 target, 而 target 不是 proxy, 不会触发 proxy 中定义的 get 方法</p>
<p>为了完成 person 对象的全面代理, 则需要设置 Reflect 的第三个参数:</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">...</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> proxy = <span class="keyword">new</span> <span class="title class_">Proxy</span>(leo, &#123;</span><br><span class="line">  <span class="title function_">get</span>(<span class="params">target, prop, receiver</span>) &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">`Access: <span class="subst">$&#123;prop&#125;</span>`</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="title class_">Reflect</span>.<span class="title function_">get</span>(target, prop, receiver);</span><br><span class="line">  &#125;,</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line">proxy.<span class="title function_">introduce</span>();</span><br></pre></td></tr></table></figure>

<p>这个操作会将使用 Reflect.get 获取 taget 内部属性的 this 也指向调用者, 而调用者则是 proxy</p>
<p>打印结果如下:</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">Access: introduce</span><br><span class="line">Access: fullName</span><br><span class="line">Access: firstName</span><br><span class="line">Access: lastName</span><br><span class="line">Hello friend, my name is Leo Messi!</span><br></pre></td></tr></table></figure>

<h2 id="Proxy-撤销"><a href="#Proxy-撤销" class="headerlink" title="Proxy 撤销"></a>Proxy 撤销</h2><p>通过 Proxy.revocable 方法创建的代理对象是可撤销代理的对象, 这种代理可以被创建者禁用, 下面是一个示例:</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">...</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> &#123; proxy, revoke &#125; = <span class="title class_">Proxy</span>.<span class="title function_">revocable</span>(leo, &#123;</span><br><span class="line">  <span class="title function_">get</span>(<span class="params">target, prop, receiver</span>) &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">`Access: <span class="subst">$&#123;prop&#125;</span>`</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="title class_">Reflect</span>.<span class="title function_">get</span>(target, prop, receiver);</span><br><span class="line">  &#125;,</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line">proxy.<span class="title function_">introduce</span>();</span><br><span class="line"><span class="title function_">revoke</span>()</span><br><span class="line">proxy.<span class="title function_">introduce</span>(<span class="string">&quot;boy&quot;</span>);</span><br></pre></td></tr></table></figure>

<p>其结果为:</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">Access: introduce</span><br><span class="line">Access: fullName</span><br><span class="line">Access: firstName</span><br><span class="line">Access: lastName</span><br><span class="line">Hello friend, my name is Leo Messi!</span><br><span class="line">Uncaught TypeError: Cannot perform <span class="string">&#x27;get&#x27;</span> on a proxy that has been revoked</span><br></pre></td></tr></table></figure>

<p>Proxy.revocable 会返回一个 revoke 方法, 调用后就可以撤销代理</p>
<h2 id="Proxy-的缺陷"><a href="#Proxy-的缺陷" class="headerlink" title="Proxy 的缺陷"></a>Proxy 的缺陷</h2><h3 id="兼容性"><a href="#兼容性" class="headerlink" title="兼容性"></a>兼容性</h3><p>作为 ES6 的特性, Proxy 对于旧版浏览器支持不好, 即使使用 proxy-polyfill 也仅能部分支持 Proxy 的功能</p>
<p>在 github 上的 GoogleChrome&#x2F;proxy-polyfill 库目前只支持以下 traps:</p>
<ul>
<li>get</li>
<li>set</li>
<li>apply</li>
<li>constructor</li>
</ul>
<h3 id="性能"><a href="#性能" class="headerlink" title="性能"></a>性能</h3><p>Proxy 的性能比 Promise 还差, 也差于 Object.defineProperty</p>
<h3 id="不能安全代理私有成员"><a href="#不能安全代理私有成员" class="headerlink" title="不能安全代理私有成员"></a>不能安全代理私有成员</h3><p>修改之前的例子:</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Person</span> &#123;</span><br><span class="line">  #firstName;</span><br><span class="line">  #lastName;</span><br><span class="line"></span><br><span class="line">  <span class="title function_">constructor</span>(<span class="params">first, last</span>) &#123;</span><br><span class="line">    <span class="variable language_">this</span>.#firstName = first;</span><br><span class="line">    <span class="variable language_">this</span>.#lastName = last;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">get</span> <span class="title function_">firstName</span>() &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="variable language_">this</span>.#firstName;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">get</span> <span class="title function_">lastName</span>() &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="variable language_">this</span>.#lastName;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">get</span> <span class="title function_">fullName</span>() &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="string">`<span class="subst">$&#123;<span class="variable language_">this</span>.firstName&#125;</span> <span class="subst">$&#123;<span class="variable language_">this</span>.lastName&#125;</span>`</span>;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="title function_">introduce</span>(<span class="params">other = <span class="string">&quot;friend&quot;</span></span>) &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">`Hello <span class="subst">$&#123;other&#125;</span>, my name is <span class="subst">$&#123;<span class="variable language_">this</span>.fullName&#125;</span>!`</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> leo = <span class="keyword">new</span> <span class="title class_">Person</span>(<span class="string">&quot;Leo&quot;</span>, <span class="string">&quot;Messi&quot;</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> proxy = <span class="keyword">new</span> <span class="title class_">Proxy</span>(leo, &#123;</span><br><span class="line">  <span class="title function_">get</span>(<span class="params">target, prop</span>) &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">`Access: <span class="subst">$&#123;prop&#125;</span>`</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="title class_">Reflect</span>.<span class="title function_">get</span>(target, prop);</span><br><span class="line">  &#125;,</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line">proxy.<span class="title function_">introduce</span>();</span><br></pre></td></tr></table></figure>

<p>现在看看打印结果:</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">Access: introduce</span><br><span class="line">Access: fullName</span><br><span class="line">Hello friend, my name is Leo Messi!</span><br></pre></td></tr></table></figure>

<p>貌似没有问题, 但如果给 Reflect 传入第三个参数, 则会出现问题:</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">...</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> proxy = <span class="keyword">new</span> <span class="title class_">Proxy</span>(leo, &#123;</span><br><span class="line">  <span class="title function_">get</span>(<span class="params">target, prop, receiver</span>) &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">`Access: <span class="subst">$&#123;prop&#125;</span>`</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="title class_">Reflect</span>.<span class="title function_">get</span>(target, prop, receiver);</span><br><span class="line">  &#125;,</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line">proxy.<span class="title function_">introduce</span>();</span><br></pre></td></tr></table></figure>

<p>打印结果为:</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">Access: introduce</span><br><span class="line">Access: fullName</span><br><span class="line">Access: firstName</span><br><span class="line">Uncaught TypeError: Cannot <span class="built_in">read</span> private member <span class="comment">#firstName from an object whose class did not declare it</span></span><br></pre></td></tr></table></figure>

<p>可以看到, 但传递 receiver 时, 调用 firstName 时 this 会指向 proxy, 而这个调用这个属性的 getter 时会指向一个私有变量, 私有变量不能在对象外获取, 则会出现这个问题</p>
<p>如果方法中使用了私有成员, 如:</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">...</span><br><span class="line">  <span class="title function_">introduce</span>(<span class="params">other = <span class="string">&quot;friend&quot;</span></span>) &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">`Hello <span class="subst">$&#123;other&#125;</span>, my name is <span class="subst">$&#123;<span class="variable language_">this</span>.#firstName&#125;</span> <span class="subst">$&#123;<span class="variable language_">this</span>.#lastName&#125;</span>!`</span>);</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<p>则可以在 Proxy 的 getter 中再次改变 this 指向来用原有对象调用该方法:</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">...</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> proxy = <span class="keyword">new</span> <span class="title class_">Proxy</span>(leo, &#123;</span><br><span class="line">  <span class="title function_">get</span>(<span class="params">target, prop, receiver</span>) &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">`Access: <span class="subst">$&#123;prop&#125;</span>`</span>);</span><br><span class="line">    <span class="keyword">const</span> value = <span class="title class_">Reflect</span>.<span class="title function_">get</span>(target, prop, receiver);</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">typeof</span> value === <span class="string">&#x27;function&#x27;</span> ? value.<span class="title function_">bind</span>(target) : value</span><br><span class="line">  &#125;,</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line">proxy.<span class="title function_">introduce</span>();</span><br></pre></td></tr></table></figure>

<p>打印结果为:</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">Access: introduce</span><br><span class="line">Hello friend, my name is Leo Messi!</span><br></pre></td></tr></table></figure>

<p>这样就能在调用使用了私有成员的方法时获取到正确的结果了</p>
]]></content>
      <categories>
        <category>Coding</category>
      </categories>
      <tags>
        <tag>Proxy</tag>
        <tag>代理</tag>
        <tag>ES6</tag>
        <tag>Proxy缺陷</tag>
        <tag>Reflect</tag>
      </tags>
  </entry>
  <entry>
    <title>ES6+ 知识点整理</title>
    <url>/2022/06/01/Coding/es-features-summary/</url>
    <content><![CDATA[<h1 id="ES6-知识点整理"><a href="#ES6-知识点整理" class="headerlink" title="ES6+ 知识点整理"></a>ES6+ 知识点整理</h1><p>介绍 ES6 及以上版本的新特性等知识点的整理笔记</p>
<h2 id="Promise"><a href="#Promise" class="headerlink" title="Promise"></a>Promise</h2><p>通过 new Promise(function) 创建 promise 对象时必须传回调函数, 否则会报错</p>
<h3 id="Promise-三种状态"><a href="#Promise-三种状态" class="headerlink" title="Promise 三种状态"></a>Promise 三种状态</h3><ol>
<li>pending 初始状态, 处理中状态</li>
<li>fulfilled 被解决状态, 调用 then 回调函数</li>
<li>rejected 被拒绝状态, 调用 catch 回调函数</li>
</ol>
<h3 id="Promise-静态方法"><a href="#Promise-静态方法" class="headerlink" title="Promise 静态方法"></a>Promise 静态方法</h3><ol>
<li>Promise.all 将多个 Promise 包装成一个, 若全部 fulfilled 则 resolve 所有 Promise, 若有一个失败则 reject</li>
<li>Promise.race 将多个 Promise 包装成一个, 若有一个 resolve 或 reject 则返回最快完成的那个 promise 实例</li>
<li>Promise.allSettled 将多个 Promise 包装成一个, 等所有 Promise 实例的状态都发生变更就执行 resolve, 无论这些 promise 的状态是成功还是失败</li>
<li>Promise.any 将多个 Promise 包装成一个, 若有一个 resolve 则返回最快完成的那个 promise 实例, 若所有都失败则 reject</li>
<li>Promise.resolve</li>
<li>Promise.reject</li>
</ol>
<h3 id="Promise-和-async-await"><a href="#Promise-和-async-await" class="headerlink" title="Promise 和 async await"></a>Promise 和 async await</h3><p>Async 是一个函数关键字, 声明一个函数是异步函数, 将返回一个 Promise 对象</p>
<p>Await 是一个运算符, 只能用于 async 函数中(也可以在代码顶层使用), 用于等待一个异步解析, 并返回解析完成后的值, 会暂停 async 函数的执行, 直到异步解析完毕后再执行后续代码</p>
<p>Promise 和 async&#x2F;await 的使用场景:</p>
<ol>
<li>如果有很多连续的依赖于前一个异步操作结果的异步操作, 则可以使用 async&#x2F;await, 这样更接近同步代码, 避免回调地狱</li>
<li>如果需要执行多个并行的异步操作并在全部处理完后再处理结果, 则 Promise 更合适</li>
</ol>
<h2 id="Symbol"><a href="#Symbol" class="headerlink" title="Symbol"></a>Symbol</h2><p>新的数据类型, 声明一个不可重复的值, 在对象中可通过 Object.getOwnPropertySymbols 或 Reflect.ownKeys 遍历出来</p>
<p>若想得到两个相等的 Symbol 则需要用 Symbol.for(key)</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> s1 = <span class="title class_">Symbol</span>.<span class="title function_">for</span>(<span class="string">&quot;123&quot;</span>);</span><br><span class="line"><span class="keyword">var</span> s2 = <span class="title class_">Symbol</span>.<span class="title function_">for</span>(<span class="string">&quot;123&quot;</span>);</span><br><span class="line"></span><br><span class="line">s1 === s2; <span class="comment">// true</span></span><br></pre></td></tr></table></figure>

<h2 id="var-let-const"><a href="#var-let-const" class="headerlink" title="var, let, const"></a>var, let, const</h2><ol>
<li>var 定义在函数内则作用域仅在函数内, 若定义在函数外(包括 if 或 for 等代码块内), 则是全局作用域, 而 let 和 const 是块作用域</li>
<li>var 变量可以在其作用域内更新和重新声明；let 变量可以更新但不能重新声明；const 变量既不能更新也不能重新声明</li>
<li>它们都被提升到了作用域的顶部, 但是, var 变量是用 undefined 初始化的, 而 let 和 const 变量不会被初始化</li>
<li>var 和 let 可以在不初始化的情况下声明, 而 const 必须在声明时初始化</li>
</ol>
<h2 id="Set-和-Map"><a href="#Set-和-Map" class="headerlink" title="Set 和 Map"></a>Set 和 Map</h2><p>Set 是由一组无序且唯一(即不能重复)的项组成的, 可以想象成集合是一个既没有重复元素, 也没有顺序概念的数组</p>
<p>Map 类似于对象, 也是键值对的集合, 但是“键”的范围不限于字符串, 各种类型的值(包括对象)都可以当作键, 是一种更完善的 Hash 结构实现. 如果你需要“键值对”的数据结构, Map 比 Object 更合适</p>
<p>共同点: 集合, 字典可以存储不重复的值<br>不同点: 集合是以[值, 值]的形式存储元素, 字典是以[键, 值]的形式存储</p>
<h2 id="Proxy"><a href="#Proxy" class="headerlink" title="Proxy"></a>Proxy</h2><p>可以给目标对象定义一个关联的代理对象, 而这个代理对象可以作为抽象的目标对象来使用, 在对目标对象的各种操作影响目标对象之前, 可以在代理对象中对这些操作加以控制. 代理对象可以作为目标对象的替身, 但又完全独立于目标对象</p>
<h2 id="动态导入和顶层-await"><a href="#动态导入和顶层-await" class="headerlink" title="动态导入和顶层 await"></a>动态导入和顶层 await</h2><p>ES2020 原生支持模块导入, 导入的模块不会污染全局命名空间</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (conditon) &#123;</span><br><span class="line">  <span class="keyword">const</span> <span class="variable language_">module</span> = <span class="keyword">await</span> <span class="keyword">import</span>(<span class="string">&quot;./myModule.js&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>并且现在可以在顶级作用域中直接使用 await</p>
<h2 id="空值合并"><a href="#空值合并" class="headerlink" title="空值合并"></a>空值合并</h2><p>?? 运算符判断变量为 null 或 undefined</p>
<h2 id="globalThis"><a href="#globalThis" class="headerlink" title="globalThis"></a>globalThis</h2><p>不论在什么环境下, globalThis 始终引用全局对象, 在浏览器是 window, 在 nodejs 是 global, 在 web worker 是 self</p>
<h2 id="Class-Fields"><a href="#Class-Fields" class="headerlink" title="Class Fields"></a>Class Fields</h2><p>类中使用#开头可以声明 private 成员, 使用 static 可以声明静态成员, 同时可以在类中用 in 关键字判断实例中是否有私有字段</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Person</span> &#123;</span><br><span class="line">  #age = <span class="number">50</span>;</span><br><span class="line">  name = <span class="string">&quot;King&quot;</span>;</span><br><span class="line">  <span class="keyword">static</span> gender = <span class="string">&quot;male&quot;</span>;</span><br><span class="line">  <span class="keyword">static</span> <span class="title function_">isPrivate</span>(<span class="params">obj</span>) &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(#age <span class="keyword">in</span> obj);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> person = <span class="keyword">new</span> <span class="title class_">Person</span>();</span><br><span class="line">person.<span class="property">name</span>; <span class="comment">// King</span></span><br><span class="line">person.#age; <span class="comment">// Uncaught SyntaxError: Private field &#x27;#age&#x27; must be declared in an enclosing class</span></span><br><span class="line">person.<span class="property">gender</span>; <span class="comment">// undefined</span></span><br><span class="line"><span class="title class_">Person</span>.<span class="property">gender</span>; <span class="comment">// male</span></span><br><span class="line"><span class="title class_">Person</span>.<span class="title function_">isPrivate</span>(person); <span class="comment">// true</span></span><br></pre></td></tr></table></figure>

<h2 id="BigInt"><a href="#BigInt" class="headerlink" title="BigInt"></a>BigInt</h2><p>JS 只能安全的表示-(2^53-1)至 2^53-1 范的值, 即 -9007199254740991 到 9007199254740991, 引入 BigInt 后可以在数字末尾加 n,表示更大的数字</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="number">9007199254740991n</span> + <span class="number">1000n</span>; <span class="comment">// 9007199254741991n</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 但不能混用BigInt和number类型</span></span><br><span class="line"><span class="number">9007199254740991n</span> + <span class="number">1000</span>; <span class="comment">// Uncaught TypeError: Cannot mix BigInt and other types, use explicit conversions</span></span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>Coding</category>
      </categories>
      <tags>
        <tag>ES6</tag>
        <tag>ES2020</tag>
        <tag>EcmaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>Nodejs 中的 Stream</title>
    <url>/2022/04/16/Coding/nodejs-stream-concept/</url>
    <content><![CDATA[<h1 id="Nodejs-中的-Stream"><a href="#Nodejs-中的-Stream" class="headerlink" title="Nodejs 中的 Stream"></a>Nodejs 中的 Stream</h1><p>详细介绍 Nodejs 中的 Stream(流) 的概念</p>
<h2 id="Stream"><a href="#Stream" class="headerlink" title="Stream"></a>Stream</h2><p>Stream 是一种抽象的数据结构, 是 Nodejs 中的一个抽象的接口, 许多对象都实现了这个接口, 如 http 服务器的 request 和 response 等</p>
<h2 id="为什么要使用-Stream"><a href="#为什么要使用-Stream" class="headerlink" title="为什么要使用 Stream"></a>为什么要使用 Stream</h2><p>在 Nodejs 中处理大文件时, 使用 fs.readFileSync 或 fs.writeFileSync 等方法会将文件整体写入内存, 而流的作用可以把这些数据拆分, 每次只写入一小部分数据, 如:</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> fs = <span class="built_in">require</span>(<span class="string">&quot;fs&quot;</span>);</span><br><span class="line"><span class="keyword">var</span> readStream = fs.<span class="title function_">createReadStream</span>(<span class="string">&quot;a.mp4&quot;</span>); <span class="comment">// 创建可读流</span></span><br><span class="line"><span class="keyword">var</span> writeStream = fs.<span class="title function_">createWriteStream</span>(<span class="string">&quot;b.mp4&quot;</span>); <span class="comment">// 创建可写流</span></span><br><span class="line"></span><br><span class="line">readStream.<span class="title function_">on</span>(<span class="string">&quot;data&quot;</span>, <span class="keyword">function</span> (<span class="params">chunk</span>) &#123;</span><br><span class="line">  <span class="comment">// 当有数据流出时，写入数据</span></span><br><span class="line">  writeStream.<span class="title function_">write</span>(chunk);</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line">readStream.<span class="title function_">on</span>(<span class="string">&quot;end&quot;</span>, <span class="keyword">function</span> (<span class="params"></span>) &#123;</span><br><span class="line">  <span class="comment">// 当没有数据时，关闭数据流</span></span><br><span class="line">  writeStream.<span class="title function_">end</span>();</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<p>当然这样写还是有问题, 读取流的速度总是快于写入流, 每次读取都触发写入可能会导致写入不能跟上读取速度, 而造成写入滞塞, 应对这个问题, 读取流还包含一个 pipe 方法可以将读取事件临时暂停, 等待写入流程完成:</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">fs.<span class="title function_">createReadStream</span>(<span class="string">&#x27;a.mp4&#x27;</span>).<span class="title function_">pipe</span>(fs.<span class="title function_">createWriteStream</span>(<span class="string">&#x27;b.mp4));</span></span><br><span class="line"><span class="string">// pipe自动调用了data,end等事件</span></span><br></pre></td></tr></table></figure>

<h2 id="Stream-来源-source"><a href="#Stream-来源-source" class="headerlink" title="Stream 来源 source"></a>Stream 来源 source</h2><p>Stream 的常见来源方式有三种：</p>
<ol>
<li>从控制台输入</li>
<li>http 请求中的 request</li>
<li>读取文件</li>
</ol>
<h2 id="Stream-输出-dest"><a href="#Stream-输出-dest" class="headerlink" title="Stream 输出 dest"></a>Stream 输出 dest</h2><p>Stream 的常见输出方式有三种：</p>
<ol>
<li>输出控制台</li>
<li>http 请求中的 response</li>
<li>写入文件</li>
</ol>
<h2 id="Stream-管道-pipe"><a href="#Stream-管道-pipe" class="headerlink" title="Stream 管道 pipe"></a>Stream 管道 pipe</h2><p>在 source 和 dest 之间有一个连接的管道 pipe,它的基本语法是 source.pipe(dest)，source 和 dest 就是通过 pipe 连接，让数据从 source 流向了 dest</p>
<h2 id="Stream-应用场景"><a href="#Stream-应用场景" class="headerlink" title="Stream 应用场景"></a>Stream 应用场景</h2><p>Stream 的应用场景主要就是处理 IO 操作，而 http 请求和文件操作都属于 IO 操作。Stream 的本质——由于一次性 IO 操作过大，硬件开销太多，影响软件运行效率，因此将 IO 分批分段进行操作，让数据像水管一样流动起来，直到流动完成，也就是操作完成</p>
<h2 id="继承自-Stream-的类"><a href="#继承自-Stream-的类" class="headerlink" title="继承自 Stream 的类"></a>继承自 Stream 的类</h2><p>以下 4 个类继承自 Stream:</p>
<ol>
<li>Readable 可读流</li>
<li>Writable 可写流</li>
<li>Duplex 可读可写流</li>
<li>Transform 在读写过程中可以修改和变换数据的 Duplex 流</li>
</ol>
]]></content>
      <categories>
        <category>Web</category>
      </categories>
      <tags>
        <tag>Nodejs</tag>
        <tag>Stream</tag>
        <tag>I/O</tag>
        <tag>Http</tag>
        <tag>Readdable</tag>
        <tag>Writable</tag>
      </tags>
  </entry>
  <entry>
    <title>Nodejs 事件循环</title>
    <url>/2022/04/16/Coding/nodejs-event-loop/</url>
    <content><![CDATA[<h1 id="Nodejs-事件循环"><a href="#Nodejs-事件循环" class="headerlink" title="Nodejs 事件循环"></a>Nodejs 事件循环</h1><p>详细介绍 Nodejs 中的 事件循环机制</p>
<h2 id="事件循环经典题目"><a href="#事件循环经典题目" class="headerlink" title="事件循环经典题目"></a>事件循环经典题目</h2><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">async1</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&quot;async1 start&quot;</span>);</span><br><span class="line">  <span class="keyword">await</span> <span class="title function_">async2</span>();</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&quot;async1 end&quot;</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">async2</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&quot;async2&quot;</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&quot;script start&quot;</span>);</span><br><span class="line"><span class="built_in">setTimeout</span>(<span class="keyword">function</span> (<span class="params"></span>) &#123;</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&quot;setTimeout0&quot;</span>);</span><br><span class="line">&#125;, <span class="number">0</span>);</span><br><span class="line"><span class="built_in">setTimeout</span>(<span class="keyword">function</span> (<span class="params"></span>) &#123;</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&quot;setTimeout3&quot;</span>);</span><br><span class="line">&#125;, <span class="number">3</span>);</span><br><span class="line"><span class="title function_">setImmediate</span>(<span class="function">() =&gt;</span> <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&quot;setImmediate&quot;</span>));</span><br><span class="line">process.<span class="title function_">nextTick</span>(<span class="function">() =&gt;</span> <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&quot;nextTick&quot;</span>));</span><br><span class="line"><span class="title function_">async1</span>();</span><br><span class="line"><span class="keyword">new</span> <span class="title class_">Promise</span>(<span class="keyword">function</span> (<span class="params">resolve</span>) &#123;</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&quot;promise1&quot;</span>);</span><br><span class="line">  <span class="title function_">resolve</span>();</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&quot;promise2&quot;</span>);</span><br><span class="line">&#125;).<span class="title function_">then</span>(<span class="keyword">function</span> (<span class="params"></span>) &#123;</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&quot;promise3&quot;</span>);</span><br><span class="line">&#125;);</span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&quot;script end&quot;</span>);</span><br></pre></td></tr></table></figure>

<p>该题目涉及 Nodejs 中所有事件循环相关概念, 弄清楚这个顺序则对于 Nodejs 的事件循环机制就有一个清晰的了解了, 这段代码的输出结果为:</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">script start</span><br><span class="line">async1 start</span><br><span class="line">async2</span><br><span class="line">promise1</span><br><span class="line">promise2</span><br><span class="line">script end</span><br><span class="line">nextTick</span><br><span class="line">async1 end</span><br><span class="line">promise3</span><br><span class="line">setTimeout0</span><br><span class="line">setImmediate</span><br><span class="line">setTimeout3</span><br></pre></td></tr></table></figure>

<h2 id="异步任务"><a href="#异步任务" class="headerlink" title="异步任务"></a>异步任务</h2><p>异步任务有两种:</p>
<ol>
<li>追加在本轮循环的异步任务</li>
<li>追加在次轮循环的异步任务</li>
</ol>
<p>本轮循环一定早于次轮循环执行</p>
<p>Nodejs 规定 process.nextTick 和 promise 的回调函数追加在本轮循环, 同步任务执行完毕后就会开始执行他们, 而 setTimeout, setInterval 和 setImmediate 等 timers 的回调函数则追加到次轮循环</p>
<h3 id="process-nextTick"><a href="#process-nextTick" class="headerlink" title="process.nextTick()"></a>process.nextTick()</h3><p>Nodejs 执行完所有同步任务, 接下来就会执行 process.nextTick 的任务队列, 属于本轮循环</p>
<h3 id="微任务-microtask"><a href="#微任务-microtask" class="headerlink" title="微任务(microtask)"></a>微任务(microtask)</h3><p>promise 的回调函数会进入异步任务里的微任务队列</p>
<p>微任务队列追加在 process.nextTick 之后, 也属于本轮循环</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="title class_">Promise</span>.<span class="title function_">resolve</span>().<span class="title function_">then</span>(<span class="function">() =&gt;</span> <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="number">1</span>));</span><br><span class="line">process.<span class="title function_">nextTick</span>(<span class="function">() =&gt;</span> <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="number">2</span>));</span><br></pre></td></tr></table></figure>

<p>以上代码始终先输出 2 再输出 1</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">process.<span class="title function_">nextTick</span>(<span class="function">() =&gt;</span> <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="number">1</span>));</span><br><span class="line"><span class="title class_">Promise</span>.<span class="title function_">resolve</span>().<span class="title function_">then</span>(<span class="function">() =&gt;</span> <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="number">2</span>));</span><br><span class="line">process.<span class="title function_">nextTick</span>(<span class="function">() =&gt;</span> <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="number">3</span>));</span><br><span class="line"><span class="title class_">Promise</span>.<span class="title function_">resolve</span>().<span class="title function_">then</span>(<span class="function">() =&gt;</span> <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="number">4</span>));</span><br></pre></td></tr></table></figure>

<p>输出结果 1, 3, 2, 4</p>
<p>开启 process.nextTick 任务会直接进入 nextTickQueue 中, 开启 promise 回调任务会直接进入 microTaskQueue 中, 只有前一个队列清空后才会执行下一个队列</p>
<h3 id="async-和-promise"><a href="#async-和-promise" class="headerlink" title="async 和 promise"></a>async 和 promise</h3><p>async 函数返回一个 promise 对象, 当函数执行时是作为一个同步函数执行, 直到遇到 await, 就会先返回, 等到执行微任务阶段完成这个异步任务, 再执行函数体内后续操作</p>
<p>而在 promise 本身函数体中的代码在 resolve 之后依然可以执行, 因为在 then 回调之前, 该函数体内还是属于同步任务阶段, 正规的写法应该不要在 resolve 或 reject 后执行任何操作</p>
<h2 id="事件循环阶段"><a href="#事件循环阶段" class="headerlink" title="事件循环阶段"></a>事件循环阶段</h2><ol>
<li>timers 阶段</li>
</ol>
<ul>
<li>此阶段包括 setTimeout 和 setInterval</li>
</ul>
<ol start="2">
<li>IO callbacks</li>
</ol>
<ul>
<li>大部分回调事件, 普通的 callback</li>
</ul>
<ol start="3">
<li>poll 阶段</li>
</ol>
<ul>
<li>网络连接, 读取文件等操作</li>
</ul>
<ol start="4">
<li>check 阶段</li>
</ol>
<ul>
<li>setImmediate</li>
</ul>
<ol start="5">
<li>close 阶段</li>
</ol>
<ul>
<li>一些 close 回调, 如 socket.on(‘close’, …) 等</li>
</ul>
<h3 id="开启事件循环"><a href="#开启事件循环" class="headerlink" title="开启事件循环"></a>开启事件循环</h3><p>Nodejs 开始执行脚本时, 会先进行事件循环初始化, 此时还没有开始事件循环, 会优先处理以下任务:</p>
<ol>
<li>同步任务</li>
<li>发出异步请求</li>
<li>规划定时器生效时间</li>
<li>执行 process.nextTick 回调</li>
<li>开始事件循环</li>
</ol>
<h3 id="setTimeout-和-setImmediate"><a href="#setTimeout-和-setImmediate" class="headerlink" title="setTimeout 和 setImmediate"></a>setTimeout 和 setImmediate</h3><p>由于 setTimeout 属于 timers 阶段, setImmediate 属于 check 阶段, 所以 setTimeout 始终早于 setImmediate 执行</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="built_in">setTimeout</span>(<span class="function">() =&gt;</span> <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="number">1</span>));</span><br><span class="line"><span class="title function_">setImmediate</span>(<span class="function">() =&gt;</span> <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="number">2</span>));</span><br></pre></td></tr></table></figure>

<p>理论上, 这段代码会先输出 1 再输出 2, 但有时也会先输出 2 再输出 1</p>
<p>因为 setTimeout 第二个参数缺省值为 0, 但 Nodejs 做不到 0 毫秒执行回调函数, 至少也要 1 毫秒, 所以 setTimeout(…, 0) 等于 setTimeout(…, 1)</p>
<p>基于系统当前状态, 进入事件循环时可能不到 1 毫秒也可能超过 1 毫秒, 如果不到 1 毫秒, 则会先进入 check 阶段, 就会先执行 setImmediate 回调函数</p>
<p>但是如果在 I&#x2F;O callbacks 阶段执行上述代码, 则会先执行 check 再执行 timers</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> fs = <span class="built_in">require</span>(<span class="string">&quot;fs&quot;</span>);</span><br><span class="line"></span><br><span class="line">fs.<span class="title function_">readFile</span>(<span class="string">&quot;aaa.json&quot;</span>, <span class="function">() =&gt;</span> &#123;</span><br><span class="line">  <span class="built_in">setTimeout</span>(<span class="function">() =&gt;</span> <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="number">1</span>));</span><br><span class="line">  <span class="title function_">setImmediate</span>(<span class="function">() =&gt;</span> <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="number">2</span>));</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<p>上述代码必然先输出 2 再输出 1</p>
]]></content>
      <categories>
        <category>Web</category>
      </categories>
      <tags>
        <tag>Nodejs</tag>
        <tag>事件循环</tag>
        <tag>微任务</tag>
        <tag>定时器</tag>
      </tags>
  </entry>
  <entry>
    <title>Rust中使用Serde进行序列化和反序列化</title>
    <url>/2022/06/03/Coding/rust-serde-tutorial/</url>
    <content><![CDATA[<h1 id="Rust-中使用-Serde-进行序列化和反序列化"><a href="#Rust-中使用-Serde-进行序列化和反序列化" class="headerlink" title="Rust 中使用 Serde 进行序列化和反序列化"></a>Rust 中使用 Serde 进行序列化和反序列化</h1><p>在 rust 中使用 serde 进行 json, bson, yaml 等格式的序列化&#x2F;反序列化处理, 并且也可以自定义序列化&#x2F;反序列化的方式</p>
<h2 id="基础"><a href="#基础" class="headerlink" title="基础"></a>基础</h2><h3 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h3><p>使用 Serde 之前需要生命依赖, 在 Cargo.toml 中声明:</p>
<figure class="highlight toml"><table><tr><td class="code"><pre><span class="line"><span class="section">[dependencies]</span></span><br><span class="line"><span class="attr">serde</span> = &#123; version = <span class="string">&quot;1.0&quot;</span>, features = [<span class="string">&quot;derive&quot;</span>] &#125;</span><br><span class="line"><span class="attr">serde_json</span> = <span class="string">&quot;1.0&quot;</span> <span class="comment"># 处理json时可以生命 serde_json</span></span><br></pre></td></tr></table></figure>

<p>features &#x3D; [“derive”]表明使用 Serde 的派生宏来自动生成序列化&#x2F;反序列化代码</p>
<h3 id="序列化"><a href="#序列化" class="headerlink" title="序列化"></a>序列化</h3><p>要用 Serde 进行序列化, 需要让 struct 实现 serde::Serialize trait, 如:</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="meta">#[derive(Serialize)]</span></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">People</span> &#123;</span><br><span class="line">  name: string</span><br><span class="line">  age: <span class="type">u32</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>然后使用 serde_json 库将 People 序列化为 json 字符串</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> serde_json</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> <span class="variable">people</span> = People &#123;</span><br><span class="line">  name: <span class="string">&quot;Pele&quot;</span>.<span class="title function_ invoke__">to_owned</span>(),</span><br><span class="line">  age: <span class="number">84</span>,</span><br><span class="line">&#125;;</span><br><span class="line"><span class="keyword">let</span> <span class="variable">json</span> = serde_json::<span class="title function_ invoke__">to_string</span>(&amp;people).<span class="title function_ invoke__">unwrap</span>();</span><br><span class="line"><span class="built_in">println!</span>(<span class="string">&quot;&#123;&#125;&quot;</span>, json); <span class="comment">// &#123;&quot;name&quot;:&quot;Pele&quot;,&quot;age&quot;:84&#125;</span></span><br></pre></td></tr></table></figure>

<h3 id="反序列化"><a href="#反序列化" class="headerlink" title="反序列化"></a>反序列化</h3><p>要用 Serde 进行反序列化, 需要让 struct 实现 serde::Deserialize trait, 如:</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="meta">#[derive(Deserialize, Debug)]</span></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">People</span> &#123;</span><br><span class="line">  name: string</span><br><span class="line">  age: <span class="type">u32</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>然后使用 serde_json 库将 json 字符串反序列化为 People</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> serde_json</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> <span class="variable">json</span> = <span class="string">r#&quot;&#123;&quot;name&quot;:&quot;Pele&quot;,&quot;age&quot;:84&#125;&quot;#</span>;</span><br><span class="line"><span class="keyword">let</span> <span class="variable">people</span> = serde_json::<span class="title function_ invoke__">from_str</span>(json).<span class="title function_ invoke__">unwrap</span>();</span><br><span class="line"><span class="built_in">println!</span>(<span class="string">&quot;&#123;:?&#125;&quot;</span>, people); <span class="comment">// People &#123; name: &quot;Pele&quot;, age: 84 &#125;</span></span><br></pre></td></tr></table></figure>

<h2 id="进阶"><a href="#进阶" class="headerlink" title="进阶"></a>进阶</h2><h3 id="自定义"><a href="#自定义" class="headerlink" title="自定义"></a>自定义</h3><p>Serde 可以自定义序列化&#x2F;反序列化方式, 如将 People 结构体在序列化时 name 值大写, 反序列化时转为小写:</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> serde::&#123;Serialize, Deserialize, Serializer, Deserializer&#125;;</span><br><span class="line"></span><br><span class="line"><span class="meta">#[derive(Serialize, Deserialize, Debug)]</span></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">People</span> &#123;</span><br><span class="line">    <span class="meta">#[serde(serialize_with = <span class="string">&quot;serialize_name&quot;</span>, deserialize_with = <span class="string">&quot;deserialize_name&quot;</span>)]</span></span><br><span class="line">    name: <span class="type">String</span>,</span><br><span class="line">    age: <span class="type">u32</span>,</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">serialize_name</span>&lt;S&gt;(name: &amp;<span class="type">String</span>, serializer: S) <span class="punctuation">-&gt;</span> <span class="type">Result</span>&lt;S::<span class="literal">Ok</span>, S::Error&gt;</span><br><span class="line"><span class="keyword">where</span></span><br><span class="line">    S: Serializer,</span><br><span class="line">&#123;</span><br><span class="line">    serializer.<span class="title function_ invoke__">serialize_str</span>(&amp;name.<span class="title function_ invoke__">to_uppercase</span>())</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">deserialize_name</span>&lt;<span class="symbol">&#x27;de</span>, D&gt;(deserializer: D) <span class="punctuation">-&gt;</span> <span class="type">Result</span>&lt;<span class="type">String</span>, D::Error&gt;</span><br><span class="line"><span class="keyword">where</span></span><br><span class="line">    D: Deserializer&lt;<span class="symbol">&#x27;de</span>&gt;,</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="variable">name</span> = <span class="type">String</span>::<span class="title function_ invoke__">deserialize</span>(deserializer)?;</span><br><span class="line">    <span class="title function_ invoke__">Ok</span>(name.<span class="title function_ invoke__">to_lowercase</span>())</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在该结构体中, 使用 #[serde(serialize_with &#x3D; “serialize_name”, deserialize_with &#x3D; “deserialize_name”)] 指定了自定义序列化&#x2F;反序列化方式</p>
<h3 id="枚举"><a href="#枚举" class="headerlink" title="枚举"></a>枚举</h3><p>Serde 同样支持枚举的序列化&#x2F;反序列化</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> serde::&#123;Serialize, Deserialize&#125;;</span><br><span class="line"></span><br><span class="line"><span class="meta">#[derive(Serialize, Deserialize, Debug)]</span></span><br><span class="line"><span class="meta">#[serde(tag = <span class="string">&quot;type&quot;</span>)]</span></span><br><span class="line"><span class="keyword">enum</span> <span class="title class_">People</span> &#123;</span><br><span class="line">    American &#123; name: <span class="type">String</span>, age: <span class="type">u32</span> &#125;,</span><br><span class="line">    Japanese &#123; name: <span class="type">String</span>, age: <span class="type">u32</span> &#125;,</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在处理时, 需要用 #[serde(tag &#x3D; “type”)] 指定枚举类型的标签</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> serde_json;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> <span class="variable">american</span> = People::American &#123; name: <span class="string">&quot;Tod&quot;</span>.<span class="title function_ invoke__">to_owned</span>(), age: <span class="number">24</span> &#125;;</span><br><span class="line"><span class="keyword">let</span> <span class="variable">json</span> = serde_json::<span class="title function_ invoke__">to_string</span>(&amp;american).<span class="title function_ invoke__">unwrap</span>();</span><br><span class="line"><span class="built_in">println!</span>(<span class="string">&quot;&#123;&#125;&quot;</span>, json); <span class="comment">// &#123;&quot;type&quot;:&quot;American&quot;,&quot;name&quot;:&quot;Tod&quot;,&quot;age&quot;:24&#125;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> <span class="variable">json</span> = <span class="string">r#&quot;&#123;&quot;type&quot;:&quot;American&quot;,&quot;name&quot;:&quot;Tod&quot;,&quot;age&quot;:24&#125;&quot;#</span>;</span><br><span class="line"><span class="keyword">let</span> <span class="variable">american</span>: People = serde_json::<span class="title function_ invoke__">from_str</span>(json).<span class="title function_ invoke__">unwrap</span>();</span><br><span class="line"><span class="built_in">println!</span>(<span class="string">&quot;&#123;:?&#125;&quot;</span>, american); <span class="comment">// American &#123; name: &quot;Tod&quot;, age: 24 &#125;</span></span><br></pre></td></tr></table></figure>

<h3 id="结构体中的-Option"><a href="#结构体中的-Option" class="headerlink" title="结构体中的 Option"></a>结构体中的 Option</h3><p>Serde 支持序列化&#x2F;反序列化结构体中的 Option 类型, 例如:</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> serde::&#123;Serialize, Deserialize&#125;;</span><br><span class="line"></span><br><span class="line"><span class="meta">#[derive(Serialize, Deserialize, Debug)]</span></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">People</span> &#123;</span><br><span class="line">    <span class="meta">#[serde(skip_serializing_if = <span class="string">&quot;Option::is_none&quot;</span>)]</span></span><br><span class="line">    name: <span class="type">Option</span>&lt;<span class="type">String</span>&gt;,</span><br><span class="line">    age: <span class="type">u32</span>,</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在处理 Option 类型时, 需要指定 #[serde(skip_serializing_if &#x3D; “Option::is_none”)], 当 Option 值为 None 时, 不进行序列化</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> serde_json;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> <span class="variable">animal</span> = Animal &#123; name: <span class="title function_ invoke__">Some</span>(<span class="string">&quot;Tod&quot;</span>.<span class="title function_ invoke__">to_owned</span>()), age: <span class="number">3</span> &#125;;</span><br><span class="line"><span class="keyword">let</span> <span class="variable">json</span> = serde_json::<span class="title function_ invoke__">to_string</span>(&amp;animal).<span class="title function_ invoke__">unwrap</span>();</span><br><span class="line"><span class="built_in">println!</span>(<span class="string">&quot;&#123;&#125;&quot;</span>, json); <span class="comment">// &#123;&quot;name&quot;:&quot;Tod&quot;,&quot;age&quot;:3&#125;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> <span class="variable">animal</span> = Animal &#123; name: <span class="literal">None</span>, age: <span class="number">3</span> &#125;;</span><br><span class="line"><span class="keyword">let</span> <span class="variable">json</span> = serde_json::<span class="title function_ invoke__">to_string</span>(&amp;animal).<span class="title function_ invoke__">unwrap</span>();</span><br><span class="line"><span class="built_in">println!</span>(<span class="string">&quot;&#123;&#125;&quot;</span>, json); <span class="comment">// &#123;&quot;age&quot;:3&#125;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> <span class="variable">json</span> = <span class="string">r#&quot;&#123;&quot;age&quot;:3&#125;&quot;#</span>;</span><br><span class="line"><span class="keyword">let</span> <span class="variable">animal</span>: Animal = serde_json::<span class="title function_ invoke__">from_str</span>(json).<span class="title function_ invoke__">unwrap</span>();</span><br><span class="line"><span class="built_in">println!</span>(<span class="string">&quot;&#123;:?&#125;&quot;</span>, animal); <span class="comment">// Animal &#123; name: None, age: 3 &#125;</span></span><br></pre></td></tr></table></figure>

<h3 id="结构体中的-Vec"><a href="#结构体中的-Vec" class="headerlink" title="结构体中的 Vec"></a>结构体中的 Vec</h3><p>Serde 支持序列化&#x2F;反序列化结构体中的 Vec 类型, 例如:</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> serde::&#123;Serialize, Deserialize&#125;;</span><br><span class="line"></span><br><span class="line"><span class="meta">#[derive(Serialize, Deserialize, Debug)]</span></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Family</span> &#123;</span><br><span class="line">  members: <span class="type">Vec</span>&lt;People&gt;,</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>Serde 会自动处理 Vec 类型</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> serde_json;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> <span class="variable">family</span> = Family &#123; members: <span class="built_in">vec!</span>[</span><br><span class="line">    People &#123; name: <span class="string">&quot;Tod&quot;</span>.<span class="title function_ invoke__">to_owned</span>(), age: <span class="number">3</span> &#125;,</span><br><span class="line">    People &#123; name: <span class="string">&quot;Curt&quot;</span>.<span class="title function_ invoke__">to_owned</span>(), age: <span class="number">2</span> &#125;,</span><br><span class="line">] &#125;;</span><br><span class="line"><span class="keyword">let</span> <span class="variable">json</span> = serde_json::<span class="title function_ invoke__">to_string</span>(&amp;family).<span class="title function_ invoke__">unwrap</span>();</span><br><span class="line"><span class="built_in">println!</span>(<span class="string">&quot;&#123;&#125;&quot;</span>, json); <span class="comment">// &#123;&quot;members&quot;:[&#123;&quot;name&quot;:&quot;Tod&quot;,&quot;age&quot;:3&#125;,&#123;&quot;name&quot;:&quot;Curt&quot;,&quot;age&quot;:2&#125;]&#125;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> <span class="variable">json</span> = <span class="string">r#&quot;&#123;&quot;members&quot;:[&#123;&quot;name&quot;:&quot;Tod&quot;,&quot;age&quot;:3&#125;,&#123;&quot;name&quot;:&quot;Curt&quot;,&quot;age&quot;:2&#125;]&#125;&quot;#</span>;</span><br><span class="line"><span class="keyword">let</span> <span class="variable">family</span>: Family = serde_json::<span class="title function_ invoke__">from_str</span>(json).<span class="title function_ invoke__">unwrap</span>();</span><br><span class="line"><span class="built_in">println!</span>(<span class="string">&quot;&#123;:?&#125;&quot;</span>, family); <span class="comment">// Family &#123; members: [People &#123; name: &quot;Tod&quot;, age: 3 &#125;, People &#123; name: &quot;Curt&quot;, age: 2 &#125;] &#125;</span></span><br></pre></td></tr></table></figure>

<h3 id="结构体中的-HashMap"><a href="#结构体中的-HashMap" class="headerlink" title="结构体中的 HashMap"></a>结构体中的 HashMap</h3><p>Serde 支持序列化&#x2F;反序列化结构体中的 HashMap 类型, 例如:</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> std::collections::HashMap;</span><br><span class="line"><span class="keyword">use</span> serde::&#123;Serialize, Deserialize&#125;;</span><br><span class="line"></span><br><span class="line"><span class="meta">#[derive(Serialize, Deserialize, Debug)]</span></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Family</span> &#123;</span><br><span class="line">    members: HashMap&lt;<span class="type">String</span>, People&gt;,</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>Serde 会自动处理 HashMap 类型</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> serde_json;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> <span class="keyword">mut </span><span class="variable">members</span> = HashMap::<span class="title function_ invoke__">new</span>();</span><br><span class="line">members.<span class="title function_ invoke__">insert</span>(<span class="string">&quot;Tod&quot;</span>.<span class="title function_ invoke__">to_owned</span>(), People &#123; name: <span class="string">&quot;Tod&quot;</span>.<span class="title function_ invoke__">to_owned</span>(), age: <span class="number">3</span> &#125;);</span><br><span class="line">members.<span class="title function_ invoke__">insert</span>(<span class="string">&quot;Curt&quot;</span>.<span class="title function_ invoke__">to_owned</span>(), People &#123; name: <span class="string">&quot;Curt&quot;</span>.<span class="title function_ invoke__">to_owned</span>(), age: <span class="number">2</span> &#125;);</span><br><span class="line"><span class="keyword">let</span> <span class="variable">family</span> = Family &#123; members &#125;;</span><br><span class="line"><span class="keyword">let</span> <span class="variable">json</span> = serde_json::<span class="title function_ invoke__">to_string</span>(&amp;family).<span class="title function_ invoke__">unwrap</span>();</span><br><span class="line"><span class="built_in">println!</span>(<span class="string">&quot;&#123;&#125;&quot;</span>, json); <span class="comment">// &#123;&quot;members&quot;:&#123;&quot;Curt&quot;:&#123;&quot;name&quot;:&quot;Curt&quot;,&quot;age&quot;:2&#125;,&quot;Tod&quot;:&#123;&quot;name&quot;:&quot;Tod&quot;,&quot;age&quot;:3&#125;&#125;&#125;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> <span class="variable">json</span> = <span class="string">r#&quot;&#123;&quot;members&quot;:&#123;&quot;Curt&quot;:&#123;&quot;name&quot;:&quot;Curt&quot;,&quot;age&quot;:2&#125;,&quot;Tod&quot;:&#123;&quot;name&quot;:&quot;Tod&quot;,&quot;age&quot;:3&#125;&#125;&#125;&quot;#</span>;</span><br><span class="line"><span class="keyword">let</span> <span class="variable">family</span>: Family = serde_json::<span class="title function_ invoke__">from_str</span>(json).<span class="title function_ invoke__">unwrap</span>();</span><br><span class="line"><span class="built_in">println!</span>(<span class="string">&quot;&#123;:?&#125;&quot;</span>, family); <span class="comment">// Family &#123; members: &#123;&quot;Tod&quot;: People &#123; name: &quot;Tod&quot;, age: 3 &#125;, &quot;Curt&quot;: People &#123; name: &quot;Curt&quot;, age: 2 &#125;&#125; &#125;</span></span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>Coding</category>
      </categories>
      <tags>
        <tag>Rust</tag>
        <tag>Serde</tag>
        <tag>序列化</tag>
        <tag>反序列化</tag>
        <tag>Json</tag>
        <tag>Yaml</tag>
      </tags>
  </entry>
  <entry>
    <title>用metamask创建你的web3身份</title>
    <url>/2022/04/15/Web3/create-web3-identity/</url>
    <content><![CDATA[<h1 id="用-metamask-创建你的-web3-身份"><a href="#用-metamask-创建你的-web3-身份" class="headerlink" title="用 metamask 创建你的 web3 身份"></a>用 metamask 创建你的 web3 身份</h1><p>介绍如何使用 metamask 创建你的第一个 web3 身份和数字钱包</p>
<h2 id="Web3-简述"><a href="#Web3-简述" class="headerlink" title="Web3 简述"></a>Web3 简述</h2><p>Web3 网络是指 Web 的下一个版本, 它被设计为一个去中心化的 Web, 旨在提供更多的隐私、安全和用户控制权。Web3 网络的核心概念是去中心化, 即不再依赖中心化的服务器和平台, 而是通过去中心化的协议和技术来实现 Web 的功能, 将为用户带来更多的隐私, 安全和数据控制权</p>
<h2 id="Metamask-简述"><a href="#Metamask-简述" class="headerlink" title="Metamask 简述"></a>Metamask 简述</h2><p>Metamask 是一款用于访问以太坊区块链上去中心化应用的浏览器插件, 绝大多数 Web3 项目都支持 Metamask</p>
<h2 id="创建第一个-web3-身份"><a href="#创建第一个-web3-身份" class="headerlink" title="创建第一个 web3 身份"></a>创建第一个 web3 身份</h2><p>创建一个 web3 身份有以下 3 步:</p>
<ol>
<li>安装 Metamask 浏览器插件</li>
<li>使用 Metamask 创建 web3 身份</li>
<li>备份 Metamask 账户</li>
</ol>
<h3 id="第一步-安装-Metamask-浏览器插件"><a href="#第一步-安装-Metamask-浏览器插件" class="headerlink" title="第一步: 安装 Metamask 浏览器插件"></a>第一步: 安装 Metamask 浏览器插件</h3><p>使用 Chrome 访问<a href="https://metamask.io/">Metamask 官方网站</a>, 点击 Download to Chrome 按钮跳转至 Chrome 插件商店, 随后点击添加至 Chrome 即可</p>
<h2 id="第二步-创建-Metamask-账户"><a href="#第二步-创建-Metamask-账户" class="headerlink" title="第二步: 创建 Metamask 账户"></a>第二步: 创建 Metamask 账户</h2><p>安装完成 Metamask 插件后, 会自动跳转到插件界面中, 此时会自动进入账户创建流程, 输入密码, 并记录下助记词, 再次输入助记词后账户创建成功</p>
<h2 id="第三步-备份-Metamask-账户"><a href="#第三步-备份-Metamask-账户" class="headerlink" title="第三步: 备份 Metamask 账户"></a>第三步: 备份 Metamask 账户</h2><p>第一次创建账户时默认会备份, 将密码和助记词等放入密码管理工具中或直接写在笔记本上, 保存在电脑上时最好进行加密储存</p>
<h2 id="Metamask-界面"><a href="#Metamask-界面" class="headerlink" title="Metamask 界面"></a>Metamask 界面</h2><p><img src="/images/create-web3-identity-01.png"></p>
<p>现在, 你已经拥有了一个 web3 身份, 这是 Metamask 的界面</p>
<p>左上角的是当前连接的网络选择, 当前连接的是以太坊主网</p>
<p>顶部是你的账号地址</p>
<p>0 ETH 是指你当前账户余额</p>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>在创建 web3 身份的过程中, 没有任何的身份验证, 这意味着你的身份在 web3 的世界中是被隐藏的, 这创造了空前的自由, 也没有任何的法律或道德, 所以在 web3 的世界中你不能相信任何人, 需要时刻保护自己</p>
]]></content>
      <categories>
        <category>Web3</category>
      </categories>
      <tags>
        <tag>Metamask</tag>
        <tag>Web3</tag>
        <tag>Web3身份</tag>
        <tag>数字钱包</tag>
      </tags>
  </entry>
  <entry>
    <title>国内镜像源(NPM, Cargo, Rustup)</title>
    <url>/2022/05/01/Tools/inland-registry-mirrors/</url>
    <content><![CDATA[<h1 id="国内镜像源-NPM-Cargo-Rustup"><a href="#国内镜像源-NPM-Cargo-Rustup" class="headerlink" title="国内镜像源(NPM, Cargo, Rustup)"></a>国内镜像源(NPM, Cargo, Rustup)</h1><p>记录各种包管理工具的国内镜像源 url 与设置方式</p>
<h2 id="NPM"><a href="#NPM" class="headerlink" title="NPM"></a>NPM</h2><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">https://registry.npmmirror.com</span><br></pre></td></tr></table></figure>

<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">npm config <span class="built_in">set</span> registry https://registry.npmmirror.com</span><br></pre></td></tr></table></figure>

<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">npm i -g pnpm --registry=https://registry.npmmirror.com</span><br></pre></td></tr></table></figure>

<h2 id="PIP"><a href="#PIP" class="headerlink" title="PIP"></a>PIP</h2><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 阿里云</span></span><br><span class="line">pip config <span class="built_in">set</span> global.index-url https://mirrors.aliyun.com/pypi/simple/</span><br><span class="line"></span><br><span class="line"><span class="comment"># 豆瓣</span></span><br><span class="line">pip config <span class="built_in">set</span> global.index-url https://pypi.douban.com/simple/</span><br><span class="line"></span><br><span class="line"><span class="comment"># 清华大学</span></span><br><span class="line">pip config <span class="built_in">set</span> global.index-url https://pypi.tuna.tsinghua.edu.cn/simple/</span><br><span class="line"></span><br><span class="line"><span class="comment"># 中国科学技术大学</span></span><br><span class="line">pip config <span class="built_in">set</span> global.index-url https://pypi.mirrors.ustc.edu.cn/simple/</span><br></pre></td></tr></table></figure>

<h2 id="Rustup"><a href="#Rustup" class="headerlink" title="Rustup"></a>Rustup</h2><p>修改环境变量 RUSTUP_DIST_SERVER (默认值为 <a href="https://static.rust-lang.org/">https://static.rust-lang.org</a>) 和 RUSTUP_UPDATE_ROOT (默认值为 <a href="https://static.rust-lang.org/rustup">https://static.rust-lang.org/rustup</a>)的值为墙内镜像源:</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 清华大学</span></span><br><span class="line">RUSTUP_DIST_SERVER=https://mirrors.tuna.tsinghua.edu.cn/rustup</span><br><span class="line"></span><br><span class="line"><span class="comment"># 中国科学技术大学</span></span><br><span class="line">RUSTUP_DIST_SERVER=https://mirrors.ustc.edu.cn/rust-static</span><br><span class="line">RUSTUP_UPDATE_ROOT=https://mirrors.ustc.edu.cn/rust-static/rustup</span><br><span class="line"></span><br><span class="line"><span class="comment"># 上海交通大学</span></span><br><span class="line">RUSTUP_DIST_SERVER=https://mirrors.sjtug.sjtu.edu.cn/rust-static/</span><br></pre></td></tr></table></figure>

<h2 id="Cargo"><a href="#Cargo" class="headerlink" title="Cargo"></a>Cargo</h2><p>打开 C:\Users&lt;用户名&gt;.cargo\config 文件, 如果没有则新建一个, 输入以下内容:</p>
<figure class="highlight toml"><table><tr><td class="code"><pre><span class="line"><span class="section">[source.crates-io]</span></span><br><span class="line"><span class="attr">registry</span> = <span class="string">&quot;https://github.com/rust-lang/crates.io-index&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 替换成要使用的镜像</span></span><br><span class="line"><span class="attr">replace-with</span> = <span class="string">&#x27;rsproxy&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 中国科学技术大学</span></span><br><span class="line"><span class="section">[source.ustc]</span></span><br><span class="line"><span class="attr">registry</span> = <span class="string">&quot;git://mirrors.ustc.edu.cn/crates.io-index&quot;</span></span><br><span class="line"><span class="comment"># 如果所处的环境中不允许使用 git 协议，可以把上述地址改为 https 协议</span></span><br><span class="line"><span class="comment">#registry = &quot;https://mirrors.ustc.edu.cn/crates.io-index&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 清华大学</span></span><br><span class="line"><span class="section">[source.tuna]</span></span><br><span class="line"><span class="attr">registry</span> = <span class="string">&quot;https://mirrors.tuna.tsinghua.edu.cn/git/crates.io-index.git&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 上海交通大学</span></span><br><span class="line"><span class="section">[source.sjtu]</span></span><br><span class="line"><span class="attr">registry</span> = <span class="string">&quot;https://mirrors.sjtug.sjtu.edu.cn/git/crates.io-index&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># rustcc 社区</span></span><br><span class="line"><span class="section">[source.rustcc]</span></span><br><span class="line"><span class="attr">registry</span> = <span class="string">&quot;git://crates.rustcc.cn/crates.io-index&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># rsproxy</span></span><br><span class="line"><span class="section">[source.rsproxy]</span></span><br><span class="line"><span class="attr">registry</span> = <span class="string">&quot;https://rsproxy.cn/crates.io-index&quot;</span></span><br><span class="line"><span class="section">[source.rsproxy-sparse]</span></span><br><span class="line"><span class="attr">registry</span> = <span class="string">&quot;sparse+https://rsproxy.cn/index/&quot;</span></span><br><span class="line"><span class="section">[registries.rsproxy]</span></span><br><span class="line"><span class="attr">index</span> = <span class="string">&quot;https://rsproxy.cn/crates.io-index&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="section">[net]</span></span><br><span class="line"><span class="attr">git-fetch-with-cli</span>=<span class="literal">true</span></span><br></pre></td></tr></table></figure>

<p>如果更换 cargo 源后执行 cargo build 命令出现如下错误:</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">blocking waiting <span class="keyword">for</span> file lock on package cache lock</span><br></pre></td></tr></table></figure>

<p>可以删除 C:\Users&lt;用户名&gt;.cargo.package-cache 文件</p>
]]></content>
      <categories>
        <category>Tools</category>
      </categories>
      <tags>
        <tag>镜像</tag>
        <tag>NPM</tag>
        <tag>Cargo</tag>
        <tag>Rustup</tag>
      </tags>
  </entry>
  <entry>
    <title>Rust安装和更新</title>
    <url>/2022/05/21/Tools/rust-installation/</url>
    <content><![CDATA[<h1 id="Rust-安装和更新"><a href="#Rust-安装和更新" class="headerlink" title="Rust 安装和更新"></a>Rust 安装和更新</h1><p>在 Windows 中安装和更新 rust, 并且配置墙内镜像源</p>
<h2 id="安装-rustup"><a href="#安装-rustup" class="headerlink" title="安装 rustup"></a>安装 rustup</h2><p>rustup 工具链需要 C++ 编译支持, 首先需要确保安装了 C++ 编译环境, 可优先下载安装<a href="https://visualstudio.microsoft.com/zh-hans/visual-cpp-build-tools/">Visual Studio</a></p>
<p>确保安装了 C++ 编译环境后, 下载并安装<a href="https://www.rust-lang.org/tools/install">Rustup</a></p>
<h2 id="配置-rustup-工具链镜像"><a href="#配置-rustup-工具链镜像" class="headerlink" title="配置 rustup 工具链镜像"></a>配置 rustup 工具链镜像</h2><p>修改环境变量 RUSTUP_DIST_SERVER (默认值为 <a href="https://static.rust-lang.org/">https://static.rust-lang.org</a>) 和 RUSTUP_UPDATE_ROOT (默认值为 <a href="https://static.rust-lang.org/rustup">https://static.rust-lang.org/rustup</a>)的值为墙内镜像源:</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 清华大学</span></span><br><span class="line">RUSTUP_DIST_SERVER=https://mirrors.tuna.tsinghua.edu.cn/rustup</span><br><span class="line"></span><br><span class="line"><span class="comment"># 中国科学技术大学</span></span><br><span class="line">RUSTUP_DIST_SERVER=https://mirrors.ustc.edu.cn/rust-static</span><br><span class="line">RUSTUP_UPDATE_ROOT=https://mirrors.ustc.edu.cn/rust-static/rustup</span><br><span class="line"></span><br><span class="line"><span class="comment"># 上海交通大学</span></span><br><span class="line">RUSTUP_DIST_SERVER=https://mirrors.sjtug.sjtu.edu.cn/rust-static/</span><br></pre></td></tr></table></figure>

<h2 id="配置-Cargo-镜像源"><a href="#配置-Cargo-镜像源" class="headerlink" title="配置 Cargo 镜像源"></a>配置 Cargo 镜像源</h2><p>打开 C:\Users&lt;用户名&gt;.cargo\config 文件, 如果没有则新建一个, 输入以下内容:</p>
<figure class="highlight toml"><table><tr><td class="code"><pre><span class="line"><span class="section">[source.crates-io]</span></span><br><span class="line"><span class="attr">registry</span> = <span class="string">&quot;https://github.com/rust-lang/crates.io-index&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 替换成要使用的镜像</span></span><br><span class="line"><span class="attr">replace-with</span> = <span class="string">&#x27;rsproxy&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 中国科学技术大学</span></span><br><span class="line"><span class="section">[source.ustc]</span></span><br><span class="line"><span class="attr">registry</span> = <span class="string">&quot;git://mirrors.ustc.edu.cn/crates.io-index&quot;</span></span><br><span class="line"><span class="comment"># 如果所处的环境中不允许使用 git 协议，可以把上述地址改为 https 协议</span></span><br><span class="line"><span class="comment">#registry = &quot;https://mirrors.ustc.edu.cn/crates.io-index&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 清华大学</span></span><br><span class="line"><span class="section">[source.tuna]</span></span><br><span class="line"><span class="attr">registry</span> = <span class="string">&quot;https://mirrors.tuna.tsinghua.edu.cn/git/crates.io-index.git&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 上海交通大学</span></span><br><span class="line"><span class="section">[source.sjtu]</span></span><br><span class="line"><span class="attr">registry</span> = <span class="string">&quot;https://mirrors.sjtug.sjtu.edu.cn/git/crates.io-index&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># rustcc 社区</span></span><br><span class="line"><span class="section">[source.rustcc]</span></span><br><span class="line"><span class="attr">registry</span> = <span class="string">&quot;git://crates.rustcc.cn/crates.io-index&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># rsproxy</span></span><br><span class="line"><span class="section">[source.rsproxy]</span></span><br><span class="line"><span class="attr">registry</span> = <span class="string">&quot;https://rsproxy.cn/crates.io-index&quot;</span></span><br><span class="line"><span class="section">[source.rsproxy-sparse]</span></span><br><span class="line"><span class="attr">registry</span> = <span class="string">&quot;sparse+https://rsproxy.cn/index/&quot;</span></span><br><span class="line"><span class="section">[registries.rsproxy]</span></span><br><span class="line"><span class="attr">index</span> = <span class="string">&quot;https://rsproxy.cn/crates.io-index&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="section">[net]</span></span><br><span class="line"><span class="attr">git-fetch-with-cli</span>=<span class="literal">true</span></span><br></pre></td></tr></table></figure>

<p>如果更换 cargo 源后执行 cargo build 命令出现如下错误:</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">blocking waiting <span class="keyword">for</span> file lock on package cache lock</span><br></pre></td></tr></table></figure>

<p>可以删除 C:\Users&lt;用户名&gt;.cargo.package-cache 文件</p>
<h2 id="rust-升级"><a href="#rust-升级" class="headerlink" title="rust 升级"></a>rust 升级</h2><p>检查是否有更新和升级 rustup</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">rustup check</span><br><span class="line"></span><br><span class="line">rustup update</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>Coding</category>
      </categories>
      <tags>
        <tag>Rust</tag>
        <tag>Cargo</tag>
        <tag>Rustup</tag>
        <tag>镜像源</tag>
      </tags>
  </entry>
  <entry>
    <title>Windows cmd 常用命令备忘</title>
    <url>/2022/04/13/Tools/win-cmd-commands/</url>
    <content><![CDATA[<h1 id="Windows-cmd-常用命令备忘"><a href="#Windows-cmd-常用命令备忘" class="headerlink" title="Windows cmd 常用命令备忘"></a>Windows cmd 常用命令备忘</h1><h2 id="cd-命令"><a href="#cd-命令" class="headerlink" title="cd 命令"></a>cd 命令</h2><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">D: <span class="comment"># 进入D盘</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">cd</span> /? <span class="comment"># 使用帮助</span></span><br><span class="line"><span class="built_in">cd</span> \  <span class="comment"># 进入当前盘根目录</span></span><br><span class="line"><span class="built_in">cd</span> C:\WINDOWS  <span class="comment"># 进入当前盘其他目录</span></span><br><span class="line"><span class="built_in">cd</span> /d E:\<span class="built_in">dir</span>  <span class="comment"># 进入其他盘某个目录, 必须加 /d 参数</span></span><br><span class="line"><span class="built_in">cd</span> ..  <span class="comment"># 跳转上一层目录</span></span><br></pre></td></tr></table></figure>

<h2 id="查看目录文件"><a href="#查看目录文件" class="headerlink" title="查看目录文件"></a>查看目录文件</h2><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line"><span class="built_in">dir</span>  <span class="comment"># 查看当前目录下的文件</span></span><br><span class="line"><span class="built_in">dir</span> /a  <span class="comment"># 查看当前目录下的包含隐藏文件的所有文件</span></span><br></pre></td></tr></table></figure>

<h2 id="创建和删除目录"><a href="#创建和删除目录" class="headerlink" title="创建和删除目录"></a>创建和删除目录</h2><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">md test_dir  <span class="comment"># 创建空目录</span></span><br><span class="line"><span class="built_in">mkdir</span> test_dir  <span class="comment"># 创建空目录</span></span><br><span class="line">rd /q/s test_dir  <span class="comment"># 删除目录, /q 表示安静模式, /s 将会删除目录树</span></span><br><span class="line"><span class="built_in">rmdir</span> /q/s test_dir  <span class="comment"># 删除目录, /q 表示安静模式, /s 将会删除目录树</span></span><br></pre></td></tr></table></figure>

<h2 id="文件处理"><a href="#文件处理" class="headerlink" title="文件处理"></a>文件处理</h2><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">copy path\to\file path\to\file <span class="comment"># 复制一个文件到另一个位置</span></span><br><span class="line">move path\to\file path\to\file <span class="comment"># 移动一个文件到另一个位置</span></span><br><span class="line">del path\to\file <span class="comment"># 删除一个文件</span></span><br></pre></td></tr></table></figure>

<h2 id="结束进程"><a href="#结束进程" class="headerlink" title="结束进程"></a>结束进程</h2><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">TASKKILL</span><br><span class="line"></span><br><span class="line">/P [password] 为提供的用户上下文指定密码。如果忽略，提示输入。</span><br><span class="line">/F 指定要强行终止的进程。</span><br><span class="line">/FI filter 指定筛选进或筛选出查询的的任务。</span><br><span class="line">/PID process <span class="built_in">id</span> 指定要终止的进程的PID。</span><br><span class="line">/IM image name 指定要终止的进程的映像名称。通配符 <span class="string">&#x27;*&#x27;</span>可用来指定所有映像名。</span><br><span class="line">/T Tree <span class="built_in">kill</span>: 终止指定的进程和任何由此启动的子进程。</span><br><span class="line">/? 显示帮助/用法。</span><br></pre></td></tr></table></figure>

<h2 id="查看网络连接状态"><a href="#查看网络连接状态" class="headerlink" title="查看网络连接状态"></a>查看网络连接状态</h2><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">netstat</span><br><span class="line"></span><br><span class="line">-a或--all：显示所有连线中的Socket；</span><br><span class="line">-A&lt;网络类型&gt;或--&lt;网络类型&gt;：列出该网络类型连线中的相关地址；</span><br><span class="line">-c或--continuous：持续列出网络状态；</span><br><span class="line">-C或--cache：显示路由器配置的快取信息；</span><br><span class="line">-e或--extend：显示网络其他相关信息；</span><br><span class="line">-F或--fib：显示FIB；</span><br><span class="line">-g或--<span class="built_in">groups</span>：显示多重广播功能群组组员名单；</span><br><span class="line">-h或--<span class="built_in">help</span>：在线帮助；</span><br><span class="line">-i或--interfaces：显示网络界面信息表单；</span><br><span class="line">-l或--listening：显示监控中的服务器的Socket；</span><br><span class="line">-M或--masquerade：显示伪装的网络连线；</span><br><span class="line">-n或--numeric：直接使用ip地址，而不通过域名服务器；</span><br><span class="line">-N或--netlink或--symbolic：显示网络硬件外围设备的符号连接名称；</span><br><span class="line">-o或--timers：显示计时器；</span><br><span class="line">-p或--programs：显示正在使用Socket的程序识别码和程序名称；</span><br><span class="line">-r或--route：显示Routing Table；</span><br><span class="line">-s或--statistice：显示网络工作信息统计表；</span><br><span class="line">-t或--tcp：显示TCP传输协议的连线状况；</span><br><span class="line">-u或--udp：显示UDP传输协议的连线状况；</span><br><span class="line">-v或--verbose：显示指令执行过程；</span><br><span class="line">-V或--version：显示版本信息；</span><br><span class="line">-w或--raw：显示RAW传输协议的连线状况；</span><br><span class="line">-x或--unix：此参数的效果和指定<span class="string">&quot;-A unix&quot;</span>参数相同；</span><br><span class="line">--ip或--inet：此参数的效果和指定<span class="string">&quot;-A inet&quot;</span>参数相同。</span><br></pre></td></tr></table></figure>

<h2 id="find"><a href="#find" class="headerlink" title="find"></a>find</h2><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">find</span><br><span class="line"></span><br><span class="line">/V         显示所有未包含指定字符串的行。</span><br><span class="line">/C         仅显示包含字符串的行数。</span><br><span class="line">/N         显示行号。</span><br><span class="line">/I         搜索字符串时忽略大小写。</span><br><span class="line">/OFF[LINE] 不要跳过具有脱机属性集的文件。</span><br><span class="line"><span class="string">&quot;string&quot;</span> 指定要搜索的文本字符串。</span><br><span class="line">[drive:][path]filename 指定要搜索的文件。</span><br><span class="line"></span><br><span class="line">如果没有指定路径，FIND 将搜索在提示符处键入</span><br><span class="line">的文本或者由另一命令产生的文本。</span><br><span class="line"></span><br><span class="line">netstat --ano | find <span class="string">&quot;:8&quot;</span> <span class="comment"># 使用管道符模糊查询</span></span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>Tools</category>
      </categories>
      <tags>
        <tag>Windows</tag>
        <tag>cmd</tag>
        <tag>命令行</tag>
        <tag>常用命令</tag>
      </tags>
  </entry>
  <entry>
    <title>用于打印的CSS</title>
    <url>/2022/04/16/Web/css-for-printing/</url>
    <content><![CDATA[<h1 id="用于打印的-CSS"><a href="#用于打印的-CSS" class="headerlink" title="用于打印的 CSS"></a>用于打印的 CSS</h1><p>介绍如何使用 CSS 来控制打印的外观</p>
<h2 id="page"><a href="#page" class="headerlink" title="@page"></a>@page</h2><p>CSS 中有一个名为 @page 的规则，它通知浏览器有关您网站打印偏好的信息。通常，我使用</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="keyword">@page</span> &#123;</span><br><span class="line">  size: Letter portrait;</span><br><span class="line">  <span class="attribute">margin</span>: <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在稍后关于边距的部分，我将解释为什么我选择了 margin: 0。您应根据与公制的关系选择使用 Letter 或 A4。</p>
<p>设置 @page 的大小和边距并不同于设置 html 或 body 元素的宽度、高度和边距。@page 位于 DOM 之外 — 它包含了 DOM。在 Web 上，html 元素受屏幕边缘限制，但在打印时受 @page 限制。</p>
<p>@page 控制的设置更多或更少对应于在按 Ctrl+P 时浏览器的打印对话框中得到的设置。</p>
<p>这是我用来进行一些实验的示例文件。</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;!DOCTYPE <span class="keyword">html</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">html</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">style</span>&gt;</span><span class="language-css"></span></span><br><span class="line"><span class="language-css">    <span class="keyword">@page</span> &#123;</span></span><br><span class="line"><span class="language-css">      <span class="comment">/* see below for each experiment */</span></span></span><br><span class="line"><span class="language-css">    &#125;</span></span><br><span class="line"><span class="language-css">    <span class="selector-tag">html</span> &#123;</span></span><br><span class="line"><span class="language-css">      <span class="attribute">width</span>: <span class="number">100%</span>;</span></span><br><span class="line"><span class="language-css">      <span class="attribute">height</span>: <span class="number">100%</span>;</span></span><br><span class="line"><span class="language-css">      <span class="attribute">background-color</span>: lightblue;</span></span><br><span class="line"><span class="language-css"></span></span><br><span class="line"><span class="language-css">      <span class="comment">/* grid by shunryu111 https://stackoverflow.com/a/32861765/5430534 */</span></span></span><br><span class="line"><span class="language-css">      <span class="attribute">background-size</span>: <span class="number">0.25in</span> <span class="number">0.25in</span>;</span></span><br><span class="line"><span class="language-css">      <span class="attribute">background-image</span>: <span class="built_in">linear-gradient</span>(to right, gray <span class="number">1px</span>, transparent <span class="number">1px</span>),</span></span><br><span class="line"><span class="language-css">        <span class="built_in">linear-gradient</span>(to bottom, gray <span class="number">1px</span>, transparent <span class="number">1px</span>);</span></span><br><span class="line"><span class="language-css">    &#125;</span></span><br><span class="line"><span class="language-css">  </span><span class="tag">&lt;/<span class="name">style</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">h1</span>&gt;</span>Sample text<span class="tag">&lt;/<span class="name">h1</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">p</span>&gt;</span>sample text<span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>在浏览器中看起来是这样的:</p>
<p><img src="/images/css-for-printing-01.png" alt="css-for-printing"></p>
<p>然后这里是写入不同 @page 值的外观:</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="keyword">@page</span> &#123;</span><br><span class="line">  size: Letter portrait;</span><br><span class="line">  <span class="attribute">margin</span>: <span class="number">1in</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="/images/css-for-printing-02.png" alt="css-for-printing"></p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="keyword">@page</span> &#123;</span><br><span class="line">  size: Letter landscape;</span><br><span class="line">  <span class="attribute">margin</span>: <span class="number">1in</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="/images/css-for-printing-03.png" alt="css-for-printing"></p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="keyword">@page</span> &#123;</span><br><span class="line">  size: Letter landscape;</span><br><span class="line">  <span class="attribute">margin</span>: <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="/images/css-for-printing-04.png" alt="css-for-printing"></p>
<p>设置 @page 的尺寸实际上不会将该尺寸的纸张放入打印机的进纸盘中。您需要自己完成这部分。</p>
<p>注意当我将尺寸设置为 A5 时，我的打印机仍保持在 Letter 尺寸，而 A5 尺寸完全适合在 Letter 尺寸内，这给人以边距的外观，尽管这并非来自于边距设置。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="keyword">@page</span> &#123;</span><br><span class="line">  size: A5 portrait;</span><br><span class="line">  <span class="attribute">margin</span>: <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="/images/css-for-printing-05.png" alt="css-for-printing"></p>
<p>但如果我告诉打印机我已加载了真实的 A5 纸张，那么打印效果就会如预期。</p>
<p><img src="/images/css-for-printing-06.png" alt="css-for-printing"></p>
<p>根据我的实验，我得出的结论是，Chrome 只有在将边距设置为默认时才遵循 @page 规则。一旦您在打印对话框中更改边距，您的输出效果就会成为您的物理纸张尺寸与所选边距的结合产品。</p>
<p>即使您选择一个完全适合您的物理纸张的 @page 尺寸，边距仍然很重要。在这里，我创建了一个没有边距的 5x5 正方形，以及一个带边距的 5x5 正方形。html 元素的尺寸由 @page 尺寸和边距的组合所限制。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="keyword">@page</span> &#123;</span><br><span class="line">  size: <span class="number">5in</span> <span class="number">5in</span>;</span><br><span class="line">  <span class="attribute">margin</span>: <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="/images/css-for-printing-07.png" alt="css-for-printing"></p>
<p>我进行所有这些测试并不是因为我期望在 A5 或 5x5 纸张上打印，而是因为我花了一些时间来弄清楚 @page 到底是什么。现在我非常有信心始终使用 Letter 并将边距设为 0</p>
<h2 id="media-print"><a href="#media-print" class="headerlink" title="@media print"></a>@media print</h2><p>有一个名为 print 的媒体查询，您可以在其中编写仅在打印时应用的样式。我的生成器页面通常包含页眉、一些选项和一些帮助用户的文本，显然这些内容不应在打印时显示，因此在这里将这些元素添加 display:none</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* Normal styles that appear while you are preparing the document */</span></span><br><span class="line"><span class="selector-tag">header</span> &#123;</span><br><span class="line">  <span class="attribute">display</span>: block;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@media</span> print &#123;</span><br><span class="line">  <span class="comment">/* Disappear when you are printing the document */</span></span><br><span class="line">  <span class="selector-tag">header</span> &#123;</span><br><span class="line">    <span class="attribute">display</span>: none;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="/images/css-for-printing-08.png" alt="css-for-printing"></p>
<h2 id="Width-height-margin-padding"><a href="#Width-height-margin-padding" class="headerlink" title="Width, height, margin, padding"></a>Width, height, margin, padding</h2><p>您需要了解一些关于盒模型的知识，以便在不费太多功夫的情况下获得您想要的边距。</p>
<p><img src="/images/css-for-printing-09.png" alt="css-for-printing"></p>
<p>我总是将 @page 的 margin 设置为 0 的原因是，我宁愿在 DOM 元素上处理边距。当我尝试使用 @page 设置 margin 为 0.5in 时，我经常会意外地得到双倍边距，导致内容比我预期的要挤压，我的单页设计溢出到第二页。</p>
<p>如果我想使用 @page 的 margin，那么实际的页面内容需要紧贴在 DOM 的边缘上，这对我来说更难思考，也更难在打印前进行预览。对我来说，在头脑中记住 html 占据整张物理纸张，而边距在 DOM 内而不是在 DOM 之外，会更容易一些。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="keyword">@page</span> &#123;</span><br><span class="line">  size: Letter portrait;</span><br><span class="line">  <span class="attribute">margin</span>: <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-tag">html</span>,</span><br><span class="line"><span class="selector-tag">body</span> &#123;</span><br><span class="line">  <span class="attribute">width</span>: <span class="number">8.5in</span>;</span><br><span class="line">  <span class="attribute">height</span>: <span class="number">11in</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在多页面打印生成器中，您需要一个表示每个页面的单独 DOM 元素。由于您不能有多个 html 或 body，您需要另一个元素。我喜欢使用 article。即使对于单页面生成器，您也可以始终使用 article。</p>
<p>由于每个 article 代表一页，我不希望在 html 或 body 上有任何边距或填充。我们将逻辑推进一步 — 对我来说，让 article 占据整张物理页面并将边距放在其中会更容易一些。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="keyword">@page</span> &#123;</span><br><span class="line">  size: Letter portrait;</span><br><span class="line">  <span class="attribute">margin</span>: <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-tag">html</span>,</span><br><span class="line"><span class="selector-tag">body</span> &#123;</span><br><span class="line">  <span class="attribute">margin</span>: <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="selector-tag">article</span> &#123;</span><br><span class="line">  <span class="attribute">width</span>: <span class="number">8.5in</span>;</span><br><span class="line">  <span class="attribute">height</span>: <span class="number">11in</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>当我谈到在我的 article 中添加边距时，我不是使用 margin 属性，而是使用 padding。这是因为 margin 在盒模型中是在元素外部和周围的。如果您使用 0.5in 的边距，您将不得不将 article 设置为 7.5×10，以便 article 加上 2× 边距等于 8.5×11。如果您想要调整边距，您将不得不调整其他尺寸。</p>
<p>相反，padding 放在元素内部，因此我可以将 article 定义为 8.5×11，带有 0.5in 的内边距，article 内的所有元素将保持在页面上。</p>
<p>当您设置 box-sizing: border-box 时，关于元素尺寸的许多直觉更容易理解。这样可以锁定 article 的外部尺寸，同时调整内部 padding。这是我的代码片段：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">html</span> &#123;</span><br><span class="line">  <span class="attribute">box-sizing</span>: border-box;</span><br><span class="line">&#125;</span><br><span class="line">*,</span><br><span class="line">*<span class="selector-pseudo">:before</span>,</span><br><span class="line">*<span class="selector-pseudo">:after</span> &#123;</span><br><span class="line">  <span class="attribute">box-sizing</span>: inherit;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>将这些代码合并起来:</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="keyword">@page</span> &#123;</span><br><span class="line">  size: Letter portrait;</span><br><span class="line">  <span class="attribute">margin</span>: <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="selector-tag">html</span> &#123;</span><br><span class="line">  <span class="attribute">box-sizing</span>: border-box;</span><br><span class="line">&#125;</span><br><span class="line">*,</span><br><span class="line">*<span class="selector-pseudo">:before</span>,</span><br><span class="line">*<span class="selector-pseudo">:after</span> &#123;</span><br><span class="line">  <span class="attribute">box-sizing</span>: inherit;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="selector-tag">html</span>,</span><br><span class="line"><span class="selector-tag">body</span> &#123;</span><br><span class="line">  <span class="attribute">margin</span>: <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="selector-tag">article</span> &#123;</span><br><span class="line">  <span class="attribute">width</span>: <span class="number">8.5in</span>;</span><br><span class="line">  <span class="attribute">height</span>: <span class="number">11in</span>;</span><br><span class="line">  <span class="attribute">padding</span>: <span class="number">0.5in</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="/images/css-for-printing-10.png" alt="css-for-printing"></p>
<h2 id="元素定位"><a href="#元素定位" class="headerlink" title="元素定位"></a>元素定位</h2><p>一旦您设置好了 article 和边距，article 内部的空间就完全由您自行决定如何使用。设计您的文档时，可以根据项目选择使用任何您认为合适的 HTML&#x2F;CSS。有时这意味着使用 flex 或 grid 布局元素，因为您在输出方面有一些灵活性。有时这意味着创建特定尺寸的方块以适合某个品牌的贴纸纸张。有时这意味着绝对定位所有内容到毫米，因为用户需要将特殊的预标记纸张放入打印机，在其上打印您的数据，而您无法控制那种特殊纸张。</p>
<p>我在这里不是为了教授如何撰写一般的 HTML 教程，所以您需要能够做到这一点。我只能说的是，请注意您正在处理的是一张纸的有限空间，而不是像浏览器窗口那样可以滚动和缩放到任意长度或比例。如果您的文档将包含任意数量的项目，请准备通过创建更多的 article 来进行分页。</p>
<h2 id="多页文档和重复元素"><a href="#多页文档和重复元素" class="headerlink" title="多页文档和重复元素"></a>多页文档和重复元素</h2><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">table</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">thead</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">tr</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">th</span>&gt;</span>Sample text<span class="tag">&lt;/<span class="name">th</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">th</span>&gt;</span>Sample text<span class="tag">&lt;/<span class="name">th</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">tr</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">thead</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">tbody</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">tr</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">td</span>&gt;</span>0<span class="tag">&lt;/<span class="name">td</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">td</span>&gt;</span>0<span class="tag">&lt;/<span class="name">td</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">tr</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">tr</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">td</span>&gt;</span>1<span class="tag">&lt;/<span class="name">td</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">td</span>&gt;</span>1<span class="tag">&lt;/<span class="name">td</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">tr</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">tr</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">td</span>&gt;</span>2<span class="tag">&lt;/<span class="name">td</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">td</span>&gt;</span>4<span class="tag">&lt;/<span class="name">td</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">tr</span>&gt;</span></span><br><span class="line">    ...</span><br><span class="line">  <span class="tag">&lt;/<span class="name">tbody</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">table</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>如果只是打印一个普通的没有复杂样式的 table 很简单，但在许多实际情况下并不是那么简单。我重新创建的文档通常每页顶部都有抬头，底部有页脚，以及其他需要在每页明确重复的自定义元素。如果只是在多页之间打印一个很长的表格，您在中间页上就没有太多机会将其他元素放在它的上方、下方或周围。</p>
<p>因此，我使用 JavaScript 生成页面，将表格分割为多个较小的部分。总体的方法如下：</p>
<ul>
<li>将 article 元素视为可丢弃的，随时准备从内存中的对象重新生成它们。所有用户输入和配置应该在单独的页眉&#x2F;选项框中进行，而不是在 article 中。</li>
<li>编写一个名为 new_page 的函数，用于创建具有必要重复的页眉&#x2F;页脚等的新 article 元素。</li>
<li>编写一个名为 renderpages 的函数，从基本数据创建 article，每次填满上一个 article 时调用 newpage。我通常使用 offsetTop 查看内容何时到达页面的较远处，尽管您肯定可以使用更智能的技术来实现每页的完美适配。</li>
<li>每当基本数据更改时调用 render_pages。</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">delete_articles</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">const</span> article <span class="keyword">of</span> <span class="title class_">Array</span>.<span class="title function_">from</span>(<span class="variable language_">document</span>.<span class="title function_">getElementsByTagName</span>(<span class="string">&quot;article&quot;</span>))) &#123;</span><br><span class="line">    <span class="variable language_">document</span>.<span class="property">body</span>.<span class="title function_">removeChild</span>(article);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">new_page</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> article = <span class="variable language_">document</span>.<span class="title function_">createElement</span>(<span class="string">&quot;article&quot;</span>);</span><br><span class="line">  article.<span class="property">innerHTML</span> = <span class="string">`</span></span><br><span class="line"><span class="string">    &lt;header&gt;...&lt;/header&gt;</span></span><br><span class="line"><span class="string">    &lt;table&gt;...&lt;/table&gt;</span></span><br><span class="line"><span class="string">    &lt;footer&gt;...&lt;/footer&gt;</span></span><br><span class="line"><span class="string">    `</span>;</span><br><span class="line">  <span class="variable language_">document</span>.<span class="property">body</span>.<span class="title function_">append</span>(article);</span><br><span class="line">  <span class="keyword">return</span> article;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">render_pages</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="title function_">delete_articles</span>();</span><br><span class="line"></span><br><span class="line">  <span class="keyword">let</span> page = <span class="title function_">new_page</span>();</span><br><span class="line">  <span class="keyword">let</span> tbody = page.<span class="title function_">query</span>(<span class="string">&quot;table tbody&quot;</span>);</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">const</span> line_item <span class="keyword">of</span> line_items) &#123;</span><br><span class="line">    <span class="comment">// I usually pick this threshold by experimentation but you can probably</span></span><br><span class="line">    <span class="comment">// do something more rigorously correct.</span></span><br><span class="line">    <span class="keyword">if</span> (tbody.<span class="property">offsetTop</span> + tbody.<span class="property">offsetParent</span>.<span class="property">offsetTop</span> &gt; <span class="number">900</span>) &#123;</span><br><span class="line">      page = <span class="title function_">new_page</span>();</span><br><span class="line">      tbody = page.<span class="title function_">query</span>(<span class="string">&quot;table tbody&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">const</span> tr = <span class="variable language_">document</span>.<span class="title function_">createElement</span>(<span class="string">&quot;tr&quot;</span>);</span><br><span class="line">    tbody.<span class="title function_">append</span>(tr);</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>通常在页面上包含一个”第 X 页，共 Y 页”的计数器是很有意义的。由于直到所有页面生成后才知道页数，所以我无法在循环中完成这个操作。我在最后调用类似下面的函数：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">renumber_pages</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="keyword">let</span> pagenumber = <span class="number">1</span>;</span><br><span class="line">  <span class="keyword">const</span> pages = <span class="variable language_">document</span>.<span class="title function_">getElementsByTagName</span>(<span class="string">&quot;article&quot;</span>);</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">const</span> page <span class="keyword">of</span> pages) &#123;</span><br><span class="line">    page.<span class="title function_">querySelector</span>(<span class="string">&quot;.pagenumber&quot;</span>).<span class="property">innerText</span> = pagenumber;</span><br><span class="line">    page.<span class="title function_">querySelector</span>(<span class="string">&quot;.totalpages&quot;</span>).<span class="property">innerText</span> = pages.<span class="property">length</span>;</span><br><span class="line">    pagenumber += <span class="number">1</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="Portrait-Landscape-模式"><a href="#Portrait-Landscape-模式" class="headerlink" title="Portrait &#x2F; Landscape 模式"></a>Portrait &#x2F; Landscape 模式</h2><p>我已经展示了 @page 规则有助于告知浏览器的默认打印设置，但用户可以在需要时进行覆盖。如果您将 @page 设置为纵向模式，而用户将其覆盖为横向模式，您的布局和分页可能看起来不正确，特别是如果您正在硬编码任何页面阈值。</p>
<p>您可以通过为纵向和横向分别创建 style 元素，并使用 JavaScript 在它们之间进行切换来满足用户需求。可能有更好的方法来实现这一点，但类似 @page 这样的 at-rules 的行为与常规 CSS 属性不同，所以我不太确定。您还应该保存一些变量，以帮助 render_pages 函数正确执行操作。</p>
<p>您也可以停止硬编码阈值，但那样我就要听从我自己的建议。</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">select</span> <span class="attr">onchange</span>=<span class="string">&quot;return page_orientation_onchange(event);&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">option</span> <span class="attr">selected</span>&gt;</span>Portrait<span class="tag">&lt;/<span class="name">option</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">option</span>&gt;</span>Landscape<span class="tag">&lt;/<span class="name">option</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">style</span> <span class="attr">id</span>=<span class="string">&quot;style_portrait&quot;</span> <span class="attr">media</span>=<span class="string">&quot;all&quot;</span>&gt;</span><span class="language-css"></span></span><br><span class="line"><span class="language-css">  <span class="keyword">@page</span> &#123;</span></span><br><span class="line"><span class="language-css">    size: Letter portrait;</span></span><br><span class="line"><span class="language-css">    <span class="attribute">margin</span>: <span class="number">0</span>;</span></span><br><span class="line"><span class="language-css">  &#125;</span></span><br><span class="line"><span class="language-css">  <span class="selector-tag">article</span> &#123;</span></span><br><span class="line"><span class="language-css">    <span class="attribute">width</span>: <span class="number">8.5in</span>;</span></span><br><span class="line"><span class="language-css">    <span class="attribute">height</span>: <span class="number">11in</span>;</span></span><br><span class="line"><span class="language-css">  &#125;</span></span><br><span class="line"><span class="language-css"></span><span class="tag">&lt;/<span class="name">style</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">style</span> <span class="attr">id</span>=<span class="string">&quot;style_landscape&quot;</span> <span class="attr">media</span>=<span class="string">&quot;not all&quot;</span>&gt;</span><span class="language-css"></span></span><br><span class="line"><span class="language-css">  <span class="keyword">@page</span> &#123;</span></span><br><span class="line"><span class="language-css">    size: Letter landscape;</span></span><br><span class="line"><span class="language-css">    <span class="attribute">margin</span>: <span class="number">0</span>;</span></span><br><span class="line"><span class="language-css">  &#125;</span></span><br><span class="line"><span class="language-css">  <span class="selector-tag">article</span> &#123;</span></span><br><span class="line"><span class="language-css">    <span class="attribute">width</span>: <span class="number">11in</span>;</span></span><br><span class="line"><span class="language-css">    <span class="attribute">height</span>: <span class="number">8.5in</span>;</span></span><br><span class="line"><span class="language-css">  &#125;</span></span><br><span class="line"><span class="language-css"></span><span class="tag">&lt;/<span class="name">style</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> print_orientation = <span class="string">&quot;portrait&quot;</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">page_orientation_onchange</span>(<span class="params">event</span>) &#123;</span><br><span class="line">  print_orientation = event.<span class="property">target</span>.<span class="property">value</span>.<span class="title function_">toLocaleLowerCase</span>();</span><br><span class="line">  <span class="keyword">if</span> (print_orientation == <span class="string">&quot;portrait&quot;</span>) &#123;</span><br><span class="line">    <span class="variable language_">document</span>.<span class="title function_">getElementById</span>(<span class="string">&quot;style_portrait&quot;</span>).<span class="title function_">setAttribute</span>(<span class="string">&quot;media&quot;</span>, <span class="string">&quot;all&quot;</span>);</span><br><span class="line">    <span class="variable language_">document</span>.<span class="title function_">getElementById</span>(<span class="string">&quot;style_landscape&quot;</span>).<span class="title function_">setAttribute</span>(<span class="string">&quot;media&quot;</span>, <span class="string">&quot;not all&quot;</span>);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">if</span> (print_orientation == <span class="string">&quot;landscape&quot;</span>) &#123;</span><br><span class="line">    <span class="variable language_">document</span>.<span class="title function_">getElementById</span>(<span class="string">&quot;style_landscape&quot;</span>).<span class="title function_">setAttribute</span>(<span class="string">&quot;media&quot;</span>, <span class="string">&quot;all&quot;</span>);</span><br><span class="line">    <span class="variable language_">document</span>.<span class="title function_">getElementById</span>(<span class="string">&quot;style_portrait&quot;</span>).<span class="title function_">setAttribute</span>(<span class="string">&quot;media&quot;</span>, <span class="string">&quot;not all&quot;</span>);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="title function_">render_printpages</span>();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">render_printpages</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="keyword">if</span> (print_orientation == <span class="string">&quot;portrait&quot;</span>) &#123;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="数据源"><a href="#数据源" class="headerlink" title="数据源"></a>数据源</h2><p>有几种方法可以将数据放在页面上。有时，我将所有数据打包到 URL 参数中，因此 JavaScript 只需执行 const urlparams &#x3D; new URLSearchParams(window.location.search); 然后一堆 urlparams.get(“title”)。这样做有一些优势：</p>
<ul>
<li>页面加载非常快。</li>
<li>通过更改 URL 来调试和实验非常容易。</li>
<li>生成器可以脱机工作。</li>
</ul>
<p>这也有一些缺点：</p>
<ul>
<li>URL 变得非常长而混乱，人们不能舒适地通过电子邮件相互发送它们。请参见本文章顶部的示例链接。</li>
<li>如果 URL 确实通过电子邮件发送，即使以后数据库中的源记录发生变化，这些数据也会“被锁定”。</li>
<li>浏览器对 URL 长度有限制。这些限制非常高，但并非无限，而且可能因客户端而异。</li>
</ul>
<p>有时候，我相反使用 JavaScript 通过 API 获取我们的数据库记录，因此 URL 参数只包含记录的主键和可能的模式设置。</p>
<p>这有一些优点：</p>
<ul>
<li>URL 长度更短。</li>
<li>数据始终是最新的。</li>
</ul>
<p>以及一些缺点：</p>
<ul>
<li>用户必须等待一会儿，直到数据被获取。</li>
<li>您需要编写更多的代码。</li>
</ul>
<p>有时我会在 article 上设置 contenteditable，以便用户在打印前进行小的更改。我也喜欢使用用户可以在打印前点击的实际复选框输入框。这些功能增加了一些便利性，但在大多数情况下，更明智的做法是让用户首先更改数据库中的源记录。此外，它们限制了您将 article 元素视为可丢弃的能力。</p>
<h2 id="完整代码"><a href="#完整代码" class="headerlink" title="完整代码"></a>完整代码</h2><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;!DOCTYPE <span class="keyword">html</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">html</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">style</span>&gt;</span><span class="language-css"></span></span><br><span class="line"><span class="language-css">    <span class="keyword">@page</span> &#123;</span></span><br><span class="line"><span class="language-css">      size: Letter portrait;</span></span><br><span class="line"><span class="language-css">      <span class="attribute">margin</span>: <span class="number">0</span>;</span></span><br><span class="line"><span class="language-css">    &#125;</span></span><br><span class="line"><span class="language-css">    <span class="selector-tag">html</span> &#123;</span></span><br><span class="line"><span class="language-css">      <span class="attribute">box-sizing</span>: border-box;</span></span><br><span class="line"><span class="language-css">    &#125;</span></span><br><span class="line"><span class="language-css">    *,</span></span><br><span class="line"><span class="language-css">    *<span class="selector-pseudo">:before</span>,</span></span><br><span class="line"><span class="language-css">    *<span class="selector-pseudo">:after</span> &#123;</span></span><br><span class="line"><span class="language-css">      <span class="attribute">box-sizing</span>: inherit;</span></span><br><span class="line"><span class="language-css">    &#125;</span></span><br><span class="line"><span class="language-css"></span></span><br><span class="line"><span class="language-css">    <span class="selector-tag">html</span>,</span></span><br><span class="line"><span class="language-css">    <span class="selector-tag">body</span> &#123;</span></span><br><span class="line"><span class="language-css">      <span class="attribute">margin</span>: <span class="number">0</span>;</span></span><br><span class="line"><span class="language-css">      <span class="attribute">background-color</span>: lightblue;</span></span><br><span class="line"><span class="language-css">    &#125;</span></span><br><span class="line"><span class="language-css"></span></span><br><span class="line"><span class="language-css">    <span class="selector-tag">header</span> &#123;</span></span><br><span class="line"><span class="language-css">      <span class="attribute">background-color</span>: white;</span></span><br><span class="line"><span class="language-css">      <span class="attribute">max-width</span>: <span class="number">8.5in</span>;</span></span><br><span class="line"><span class="language-css">      <span class="attribute">margin</span>: <span class="number">8px</span> auto;</span></span><br><span class="line"><span class="language-css">      <span class="attribute">padding</span>: <span class="number">8px</span>;</span></span><br><span class="line"><span class="language-css">    &#125;</span></span><br><span class="line"><span class="language-css"></span></span><br><span class="line"><span class="language-css">    <span class="selector-tag">article</span> &#123;</span></span><br><span class="line"><span class="language-css">      <span class="attribute">background-color</span>: white;</span></span><br><span class="line"><span class="language-css">      <span class="attribute">padding</span>: <span class="number">0.5in</span>;</span></span><br><span class="line"><span class="language-css">      <span class="attribute">width</span>: <span class="number">8.5in</span>;</span></span><br><span class="line"><span class="language-css">      <span class="attribute">height</span>: <span class="number">11in</span>;</span></span><br><span class="line"><span class="language-css"></span></span><br><span class="line"><span class="language-css">      <span class="comment">/* For centering the page on the screen during preparation */</span></span></span><br><span class="line"><span class="language-css">      <span class="attribute">margin</span>: <span class="number">8px</span> auto;</span></span><br><span class="line"><span class="language-css">    &#125;</span></span><br><span class="line"><span class="language-css"></span></span><br><span class="line"><span class="language-css">    <span class="keyword">@media</span> print &#123;</span></span><br><span class="line"><span class="language-css">      <span class="selector-tag">html</span>,</span></span><br><span class="line"><span class="language-css">      <span class="selector-tag">body</span> &#123;</span></span><br><span class="line"><span class="language-css">        <span class="attribute">background-color</span>: white <span class="meta">!important</span>;</span></span><br><span class="line"><span class="language-css">      &#125;</span></span><br><span class="line"><span class="language-css">      <span class="selector-tag">body</span> &gt; <span class="selector-tag">header</span> &#123;</span></span><br><span class="line"><span class="language-css">        <span class="attribute">display</span>: none;</span></span><br><span class="line"><span class="language-css">      &#125;</span></span><br><span class="line"><span class="language-css">      <span class="selector-tag">article</span> &#123;</span></span><br><span class="line"><span class="language-css">        <span class="attribute">margin</span>: <span class="number">0</span> <span class="meta">!important</span>;</span></span><br><span class="line"><span class="language-css">      &#125;</span></span><br><span class="line"><span class="language-css">    &#125;</span></span><br><span class="line"><span class="language-css">  </span><span class="tag">&lt;/<span class="name">style</span>&gt;</span></span><br><span class="line"></span><br><span class="line">  <span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">header</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">p</span>&gt;</span>Some help text to explain the purpose of this generator.<span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">p</span>&gt;</span><span class="tag">&lt;<span class="name">button</span> <span class="attr">onclick</span>=<span class="string">&quot;return window.print();&quot;</span>&gt;</span>Print<span class="tag">&lt;/<span class="name">button</span>&gt;</span><span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">header</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">article</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">h1</span>&gt;</span>Sample page 1<span class="tag">&lt;/<span class="name">h1</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">p</span>&gt;</span>sample text<span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">article</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">article</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">h1</span>&gt;</span>Sample page 2<span class="tag">&lt;/<span class="name">h1</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">p</span>&gt;</span>sample text<span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">article</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>Web</category>
      </categories>
      <tags>
        <tag>HTML</tag>
        <tag>CSS</tag>
        <tag>打印</tag>
        <tag>媒体查询</tag>
      </tags>
  </entry>
  <entry>
    <title>React 细节整理</title>
    <url>/2022/05/01/Web/react-details/</url>
    <content><![CDATA[<h1 id="React-细节整理"><a href="#React-细节整理" class="headerlink" title="React 细节整理"></a>React 细节整理</h1><p>实际使用中遇到的一些 React 操作的细节和问题的记录</p>
<h2 id="为什么开发环境中在挂在组件时使用-useEffect-会触发两次副作用"><a href="#为什么开发环境中在挂在组件时使用-useEffect-会触发两次副作用" class="headerlink" title="为什么开发环境中在挂在组件时使用 useEffect 会触发两次副作用"></a>为什么开发环境中在挂在组件时使用 useEffect 会触发两次副作用</h2><p>A: React 18 开始, 在 development 模式, 且使用了 strict mode 情况下, 挂载组件的 useEffect 会执行两次, 为了方便开发者模拟挂载后立即卸载组件, 可以提前发现重复挂载是否出现 bug</p>
<h2 id="组件的-key-有什么作用"><a href="#组件的-key-有什么作用" class="headerlink" title="组件的 key 有什么作用"></a>组件的 key 有什么作用</h2><p>A: key 涉及到组件树的 diff 算法, diff 有如下规则:</p>
<ol>
<li>当元素类型变化时，会销毁重建</li>
<li>当元素类型不变时，对比属性</li>
<li>当组件元素类型不变时，通过 props 递归判断子节点</li>
<li>递归对比子节点，当子节点是列表时，通过 key 和 props 来判断。若 key 一致，则进行更新，若 key 不一致，就销毁重建</li>
</ol>
<p>key 的取值分为三种: 不定值, 索引值, 唯一值</p>
<ol>
<li>不定值: 如 Math.random(), 在列表渲染中如果修改列表值或者仅仅只是 setState 一个一样的列表, 也会导致真实 dom 被销毁</li>
<li>索引值: 若使用列表索引作为 key, 在列表顺序被打乱的情况下不会销毁真实 dom, 但可能会导致非受控组件(如 input)互相篡改导致无法预期的变化</li>
<li>唯一值: 推荐使用唯一值作为 key, 以保证 key 的唯一性和确定性</li>
</ol>
<h2 id="useMemo-和-memo-的区别"><a href="#useMemo-和-memo-的区别" class="headerlink" title="useMemo 和 memo 的区别"></a>useMemo 和 memo 的区别</h2><p>useMemo 和 memo 都可用于缓存组件, 区别在于:</p>
<ol>
<li>useMemo 可以缓存状态, memo 是一个 HOC, 只能缓存组件</li>
<li>useMemo 需要声明依赖项, memo 默认依赖于组件的 props</li>
</ol>
<h2 id="组件缓存方式"><a href="#组件缓存方式" class="headerlink" title="组件缓存方式"></a>组件缓存方式</h2><ol>
<li>使用 React.memo 或 useMemo 来对组件进行缓存</li>
<li>子组件通过 props.children 来挂载, 能够保证子组件不更新, 这种方式传递给一个组件的子组件实际上是属于父组件的子组件, 所以当前组件更新不会触发这个子组件更新</li>
</ol>
<h2 id="useLayoutEffect-与-useEffect"><a href="#useLayoutEffect-与-useEffect" class="headerlink" title="useLayoutEffect 与 useEffect"></a>useLayoutEffect 与 useEffect</h2><p>useLayoutEffect 与 useEffect 作用和语法都一样, 区别只有执行时机:</p>
<ol>
<li>useEffect 执行在 React 渲染和提交之后, 是异步执行的, 可能会让界面的改动在下一次绘制才生效</li>
<li>useLayoutEffect 执行在浏览器绘制之前, 是同步执行的, 会阻塞渲染, 等价于 ComponentDidMount (源码中有调用实例的 ComponentDidMount 钩子), 并且在 ssr 模式下不能使用</li>
</ol>
<p>总结:</p>
<ol>
<li>优先使用 useEffect, 因为它是异步不会阻塞渲染</li>
<li>会影响渲染的操作可以放到 useLayoutEffect 中, 避免闪烁</li>
<li>不能在 ssr 中使用 useLayoutEffect</li>
</ol>
<h2 id="useCallback-作用"><a href="#useCallback-作用" class="headerlink" title="useCallback 作用"></a>useCallback 作用</h2><p>useCallback 可以缓存函数, 是对于 useMemo 功能的特化</p>
<h3 id="使用场景"><a href="#使用场景" class="headerlink" title="使用场景"></a>使用场景</h3><p>想象一个场景, 当有一个 React.memo 创建的子组件, 它的 prop 有一个值为函数, 来自父组件传递, 而这个函数是定义在父组件中的, 那么父组件每次渲染时, 都会创建一个新的函数实例导致子组件不必要的重新渲染, 而 useCallback 就是为了解决这个问题</p>
<p>useCallback 不是总会带来性能提升, 应该避免过度优化</p>
<p>应该使用的场景:</p>
<ol>
<li>子组件的性能优化: 当将一个函数传递给已经用 React.memo 优化过的子组件时, 使用 useCallback 可以确保子组件不会因为父组件重新渲染导致子组件不必要的重新渲染</li>
<li>Hook 依赖: 若函数被作为其他 hook 的依赖项, 使用 useCallback 可确保函数的稳定性, 可以避免不必要的副作用</li>
<li>复杂计算和频繁的重新渲染: 如果组件涉及频繁的操作和反馈, 使用 useCallback 可以避免性能问题</li>
</ol>
<p>不应使用的场景:</p>
<ol>
<li>不涉及传参和作为依赖的情况</li>
<li>简单组件</li>
<li>过度优化: 通常函数组件的重新渲染不会带来明显的性能问题, useCallback 没有必要</li>
</ol>
<h2 id="useTransition"><a href="#useTransition" class="headerlink" title="useTransition"></a>useTransition</h2><h3 id="使用"><a href="#使用" class="headerlink" title="使用"></a>使用</h3><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> [isPending, startTransition] = <span class="title function_">useTransition</span>();</span><br><span class="line"></span><br><span class="line"><span class="title function_">startTransition</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">  <span class="title function_">setPage</span>(<span class="string">&quot;/about&quot;</span>);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<h3 id="注意点"><a href="#注意点" class="headerlink" title="注意点"></a>注意点</h3><ol>
<li>useTransition 在 react 18 中加入, 需要通过 React.createRoot(root).render(<app />) 开启并发模式才能使用</li>
<li>startTransition 中的函数必须是同步的</li>
<li>被 useTransition 包裹的同一个状态多次更新, 只会渲染最后一个, 前面的都算中断</li>
</ol>
<h2 id="useDefferdValue"><a href="#useDefferdValue" class="headerlink" title="useDefferdValue"></a>useDefferdValue</h2><h3 id="使用-1"><a href="#使用-1" class="headerlink" title="使用"></a>使用</h3><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> [value, setValue] = <span class="title function_">useState</span>();</span><br><span class="line"><span class="keyword">const</span> deferredText = <span class="title function_">useDeferredValue</span>(value);</span><br></pre></td></tr></table></figure>

<h3 id="特性"><a href="#特性" class="headerlink" title="特性"></a>特性</h3><p>类似于 useTransition 是可中断的单个状态, 会在其他紧急任务完毕后才执行, 并且每次只渲染最新的结果</p>
<h3 id="与节流防抖的区别"><a href="#与节流防抖的区别" class="headerlink" title="与节流防抖的区别"></a>与节流防抖的区别</h3><ol>
<li>主要应用于优化界面的渲染, 不能终止如网络请求等操作</li>
<li>不用设置固定的延迟时间, 会自动在空闲时间执行</li>
</ol>
<h2 id="父组件获取子组件的状态和方法的方式"><a href="#父组件获取子组件的状态和方法的方式" class="headerlink" title="父组件获取子组件的状态和方法的方式"></a>父组件获取子组件的状态和方法的方式</h2><ol>
<li>父组件将 ref 传递给子组件, 子组件用 forwardRef 包裹, 这样可以获取到子组件的元素</li>
<li>在子组件中使用 useImperativeHandle 返回定义好的属性和方法, 并使用 forwardRef 包裹, 这样父组件就能通过 ref 调用子组件定义好的属性和方法</li>
</ol>
<h2 id="useEffect-依赖对象深层比较"><a href="#useEffect-依赖对象深层比较" class="headerlink" title="useEffect 依赖对象深层比较"></a>useEffect 依赖对象深层比较</h2><p>useEffect 对引用类型的依赖比较是浅比较(Object.is(obj1, obj2) 进行比较), 只要对象指针变化了就会触发副作用, 如果要深层比较对象字面量需要自定义 hooks, 如:</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; isEqual &#125; <span class="keyword">from</span> <span class="string">&quot;lodash&quot;</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> effect 副作用函数</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> deps 依赖项, 可以是引用类型的值</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> compare 比较函数, 用于对象深层比较</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">const</span> <span class="title function_">useDeepCompareEffect</span> = (<span class="params">effect, deps, compare</span>) =&gt; &#123;</span><br><span class="line">  <span class="comment">// 如果没传比较函数, 则默认使用lodash的isEqual</span></span><br><span class="line">  <span class="keyword">if</span> (!compare) compare = isEqual;</span><br><span class="line">  <span class="comment">// 标记是否改变</span></span><br><span class="line">  <span class="keyword">const</span> signal = useRef&lt;<span class="built_in">number</span>&gt;(<span class="number">0</span>);</span><br><span class="line">  <span class="keyword">const</span> memoizedDeps = useRef&lt;<span class="built_in">any</span>&gt;([]);</span><br><span class="line">  <span class="keyword">if</span> (deps === <span class="literal">undefined</span> || !<span class="title function_">compare</span>(memoizedDeps.<span class="property">current</span>, deps)) &#123;</span><br><span class="line">    signal.<span class="property">current</span>++;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">// 缓存当前deps</span></span><br><span class="line">  memoizedDeps.<span class="property">current</span> = deps;</span><br><span class="line">  <span class="title function_">useEffect</span>(effect, [signal.<span class="property">current</span>]);</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h2 id="高阶组件使用场景"><a href="#高阶组件使用场景" class="headerlink" title="高阶组件使用场景"></a>高阶组件使用场景</h2><ol>
<li>复用逻辑: HOC 可以帮助我们在组件之间复用逻辑, 避免重复代码, 如带 loading 状态的多个组件可以将 loading 逻辑放入 HOC 来避免在每个组件都声明 loading 状态</li>
<li>修改 props: HOC 可以修改传递给组件的 props, 从而改变组件的行为, 如利用 HOC 根据权限显示或隐藏某些部分</li>
<li>条件渲染: 可以根据条件来确定是渲染传入的组件还是其他组件</li>
<li>提供额外功能: 可以为组件提供额外的功能, 如错误处理, 数据处理, 性能监控等</li>
</ol>
]]></content>
      <categories>
        <category>Web</category>
      </categories>
      <tags>
        <tag>React</tag>
        <tag>React 细节</tag>
      </tags>
  </entry>
  <entry>
    <title>React 事件机制原理</title>
    <url>/2022/06/15/Web/react-events-system/</url>
    <content><![CDATA[<h1 id="React-事件机制原理"><a href="#React-事件机制原理" class="headerlink" title="React 事件机制原理"></a>React 事件机制原理</h1><p>详细介绍 React 中的合成事件机制和实现原理</p>
<h2 id="原生事件"><a href="#原生事件" class="headerlink" title="原生事件"></a>原生事件</h2><p>浏览器基于 W3C 规范实现了一套标准化 DOM 事件, 基于 Event 类实现了常见的用户事件如 InputEvent, MouseEvent 等</p>
<p>事件发生时, 相关信息会储存在 Event 实例对象中, 包括 currentTarget, detail, target, preventDefault(), stopPropagation() 等属性方法, DOM 节点可以通过 addEventListener 和 removeEventListener 来添加或移除事件监听函数</p>
<h2 id="React-合成事件"><a href="#React-合成事件" class="headerlink" title="React 合成事件"></a>React 合成事件</h2><p>React 在基于原生事件规范的前提下实现了合成事件(Synthetic Events), 合成事件与原生事件不是一一对应关系, 如 onChange 事件就由 change, click, input, keydown, keyup 等原生事件组成</p>
<h2 id="事件机制"><a href="#事件机制" class="headerlink" title="事件机制"></a>事件机制</h2><h3 id="事件注册"><a href="#事件注册" class="headerlink" title="事件注册"></a>事件注册</h3><p>使用 React.createRoot 创建 root 时, React 会调用 listenToAllSupportedEvents 方法对所有支持的原生事件进行监听, 该方法会收集并返回 allNativeEvents, 用于收集所有合成事件相关联的原生事件名, 并且这个收集动作在事件插件初始化阶段就完成了</p>
<p>随后对每个原生事件调用 addTrappedEventListener 函数, 函数内最终使用 addEventListener 方法对原生事件进行捕获或冒泡阶段的事件监听注册</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">addTrappedEventListener</span>(<span class="params"></span></span><br><span class="line"><span class="params">  targetContainer: EventTarget,</span></span><br><span class="line"><span class="params">  domEventName: DOMEventName,</span></span><br><span class="line"><span class="params">  eventSystemFlags: EventSystemFlags,</span></span><br><span class="line"><span class="params">  isCapturePhaseListener: boolean</span></span><br><span class="line"><span class="params"></span>) &#123;</span><br><span class="line">  <span class="keyword">let</span> listener = <span class="title function_">createEventListenerWrapperWithPriority</span>(</span><br><span class="line">    targetContainer,</span><br><span class="line">    domEventName,</span><br><span class="line">    eventSystemFlags</span><br><span class="line">  );</span><br><span class="line"></span><br><span class="line">  <span class="comment">// ...</span></span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (isCapturePhaseListener) &#123;</span><br><span class="line">    <span class="title function_">addEventCaptureListener</span>(targetContainer, domEventName, listener);</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="title function_">addEventBubbleListener</span>(targetContainer, domEventName, listener);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>基于这个流程可知, 调用 React.createRoot 时就已经在 root 节点上初始化了所有原生事件的监听回调函数, 而不是在执行组件函数时才进行注册</p>
<h3 id="事件触发"><a href="#事件触发" class="headerlink" title="事件触发"></a>事件触发</h3><p><img src="/images/react-events-system-01.png" alt="react-events-system-01"></p>
<p>在注册事件阶段调用的 addTrappedEventListener 方法中, 会使用 createEventListenerWrapperWithPriority 函数来创建事件回调. createEventListenerWrapperWithPriority 函数根据事件类型, 划分出若干个不同优先级的 dispathEvent. 事件回调最终都调用进 dispatchEvent 方法</p>
<p>触发一个事件的执行流程如下:</p>
<ol>
<li>原生事件触发后进入 dispathEvent 回调方法</li>
<li>attemptToDispatchEvent 方法根据该原生事件查找到当前 DOM 节点和映射的 Fiber 节点</li>
<li>事件和 Fiber 等信息被派发给插件系统进行处理, 插件系统调用各插件暴露的 extractEvents 方法</li>
<li>accumulateSinglePhaseListeners 方法向上收集 Fiber 树上监听相关事件的其他回调函数, 构造合成事件并加入到派发队列 dispatchQueue 中</li>
<li>调用 processDispatchQueue 方法, 基于捕获或冒泡阶段的标识, 按倒序或顺序执行 dispatchQueue 中的方法</li>
</ol>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>React 的事件处理机制可以分为两个阶段, React.createRoot 时在 root 节点上注册原生事件, 原生事件触发时模拟捕获, 目标和冒泡阶段派发合成事件. 通过这种机制, 冒泡的原生事件类型最多在 root 节点上注册一次, 节省了内存开销. 且 React 为不同类型的事件定义了不同的优先级, 让代码及时响应高优先级的用户交互, 提升用户体验</p>
<p>React 的合成事件在符合 W3C 规范的前提下抹平了不同浏览器的差异, 并且简化事件逻辑, 对关联事件进行合成</p>
]]></content>
      <categories>
        <category>Web</category>
      </categories>
      <tags>
        <tag>React</tag>
        <tag>事件</tag>
        <tag>原生事件</tag>
        <tag>合成事件</tag>
        <tag>事件机制</tag>
        <tag>事件系统</tag>
      </tags>
  </entry>
  <entry>
    <title>React Fiber</title>
    <url>/2022/06/15/Web/react-fiber/</url>
    <content><![CDATA[<h1 id="React-Fiber"><a href="#React-Fiber" class="headerlink" title="React Fiber"></a>React Fiber</h1><p>详细介绍 React 中的 fiber 机制和原理</p>
<h2 id="什么是-Fiber"><a href="#什么是-Fiber" class="headerlink" title="什么是 Fiber"></a>什么是 Fiber</h2><p>Fiber 就是一个 js 对象, 用于描述一个 react 工作单元. 早期的 react 中是使用虚拟 DOM 来进行描述, 现在的 fiber 架构中则是用 fiber, fiber 可以理解为一个更强大的虚拟 DOM</p>
<p>一个简化的 fiber 对象如下:</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">  <span class="attr">type</span>: <span class="string">&#x27;h1&#x27;</span>,  <span class="comment">// 组件类型</span></span><br><span class="line">  <span class="attr">key</span>: <span class="literal">null</span>,   <span class="comment">// React key</span></span><br><span class="line">  <span class="attr">props</span>: &#123; ... &#125;, <span class="comment">// 输入的props</span></span><br><span class="line">  <span class="attr">state</span>: &#123; ... &#125;, <span class="comment">// 组件的state (如果是class组件或带有state的function组件)</span></span><br><span class="line">  <span class="attr">child</span>: <span class="title class_">Fiber</span> | <span class="literal">null</span>,  <span class="comment">// 第一个子元素的Fiber</span></span><br><span class="line">  <span class="attr">sibling</span>: <span class="title class_">Fiber</span> | <span class="literal">null</span>,  <span class="comment">// 下一个兄弟元素的Fiber</span></span><br><span class="line">  <span class="attr">return</span>: <span class="title class_">Fiber</span> | <span class="literal">null</span>,  <span class="comment">// 父元素的Fiber</span></span><br><span class="line">  <span class="comment">// ...其他属性</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>React 工作时, 会沿着 fiber 树形结构进行, 对比每个 fiber 旧的 props 和新的 props 来确定是否需要更新组件, 如果主线程有更重要的工作, 如响应用户输入, 则可以中断当前工作并返回主线程上的任务</p>
<h2 id="Fiber-结构"><a href="#Fiber-结构" class="headerlink" title="Fiber 结构"></a>Fiber 结构</h2><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">FiberNode</span>(<span class="params"></span></span><br><span class="line"><span class="params">  <span class="variable language_">this</span>: $FlowFixMe,</span></span><br><span class="line"><span class="params">  tag: WorkTag,</span></span><br><span class="line"><span class="params">  pendingProps: mixed,</span></span><br><span class="line"><span class="params">  key: <span class="literal">null</span> | string,</span></span><br><span class="line"><span class="params">  mode: TypeOfMode</span></span><br><span class="line"><span class="params"></span>) &#123;</span><br><span class="line">  <span class="comment">// 基本属性</span></span><br><span class="line">  <span class="variable language_">this</span>.<span class="property">tag</span> = tag; <span class="comment">// 描述此Fiber的启动模式的值（LegacyRoot = 0; ConcurrentRoot = 1）</span></span><br><span class="line">  <span class="variable language_">this</span>.<span class="property">key</span> = key; <span class="comment">// React key</span></span><br><span class="line">  <span class="variable language_">this</span>.<span class="property">elementType</span> = <span class="literal">null</span>; <span class="comment">// 描述React元素的类型。例如，对于JSX&lt;App /&gt;，elementType是App</span></span><br><span class="line">  <span class="variable language_">this</span>.<span class="property">type</span> = <span class="literal">null</span>; <span class="comment">// 组件类型</span></span><br><span class="line">  <span class="variable language_">this</span>.<span class="property">stateNode</span> = <span class="literal">null</span>; <span class="comment">// 对于类组件，这是类的实例；对于DOM元素，它是对应的DOM节点。</span></span><br><span class="line"></span><br><span class="line">  <span class="comment">// Fiber链接</span></span><br><span class="line">  <span class="variable language_">this</span>.<span class="property">return</span> = <span class="literal">null</span>; <span class="comment">// 指向父Fiber</span></span><br><span class="line">  <span class="variable language_">this</span>.<span class="property">child</span> = <span class="literal">null</span>; <span class="comment">// 指向第一个子Fiber</span></span><br><span class="line">  <span class="variable language_">this</span>.<span class="property">sibling</span> = <span class="literal">null</span>; <span class="comment">// 指向其兄弟Fiber</span></span><br><span class="line">  <span class="variable language_">this</span>.<span class="property">index</span> = <span class="number">0</span>; <span class="comment">// 子Fiber中的索引位置</span></span><br><span class="line"></span><br><span class="line">  <span class="variable language_">this</span>.<span class="property">ref</span> = <span class="literal">null</span>; <span class="comment">// 如果组件上有ref属性，则该属性指向它</span></span><br><span class="line">  <span class="variable language_">this</span>.<span class="property">refCleanup</span> = <span class="literal">null</span>; <span class="comment">// 如果组件上的ref属性在更新中被删除或更改，此字段会用于追踪需要清理的旧ref</span></span><br><span class="line"></span><br><span class="line">  <span class="comment">// Props &amp; State</span></span><br><span class="line">  <span class="variable language_">this</span>.<span class="property">pendingProps</span> = pendingProps; <span class="comment">// 正在等待处理的新props</span></span><br><span class="line">  <span class="variable language_">this</span>.<span class="property">memoizedProps</span> = <span class="literal">null</span>; <span class="comment">// 上一次渲染时的props</span></span><br><span class="line">  <span class="variable language_">this</span>.<span class="property">updateQueue</span> = <span class="literal">null</span>; <span class="comment">// 一个队列，包含了该Fiber上的状态更新和副作用</span></span><br><span class="line">  <span class="variable language_">this</span>.<span class="property">memoizedState</span> = <span class="literal">null</span>; <span class="comment">// 上一次渲染时的state</span></span><br><span class="line">  <span class="variable language_">this</span>.<span class="property">dependencies</span> = <span class="literal">null</span>; <span class="comment">// 该Fiber订阅的上下文或其他资源的描述</span></span><br><span class="line"></span><br><span class="line">  <span class="comment">// 工作模式</span></span><br><span class="line">  <span class="variable language_">this</span>.<span class="property">mode</span> = mode; <span class="comment">// 描述Fiber工作模式的标志（例如Concurrent模式、Blocking模式等）。</span></span><br><span class="line"></span><br><span class="line">  <span class="comment">// Effects</span></span><br><span class="line">  <span class="variable language_">this</span>.<span class="property">flags</span> = <span class="title class_">NoFlags</span>; <span class="comment">// 描述该Fiber发生的副作用的标志（十六进制的标识）</span></span><br><span class="line">  <span class="variable language_">this</span>.<span class="property">subtreeFlags</span> = <span class="title class_">NoFlags</span>; <span class="comment">// 描述该Fiber子树中发生的副作用的标志（十六进制的标识）</span></span><br><span class="line">  <span class="variable language_">this</span>.<span class="property">deletions</span> = <span class="literal">null</span>; <span class="comment">// 在commit阶段要删除的子Fiber数组</span></span><br><span class="line"></span><br><span class="line">  <span class="variable language_">this</span>.<span class="property">lanes</span> = <span class="title class_">NoLanes</span>; <span class="comment">// 与React的并发模式有关的调度概念。</span></span><br><span class="line">  <span class="variable language_">this</span>.<span class="property">childLanes</span> = <span class="title class_">NoLanes</span>; <span class="comment">// 与React的并发模式有关的调度概念。</span></span><br><span class="line"></span><br><span class="line">  <span class="variable language_">this</span>.<span class="property">alternate</span> = <span class="literal">null</span>; <span class="comment">// Current Tree和Work-in-progress (WIP) Tree的互相指向对方tree里的对应单元</span></span><br><span class="line"></span><br><span class="line">  <span class="comment">// 如果启用了性能分析</span></span><br><span class="line">  <span class="keyword">if</span> (enableProfilerTimer) &#123;</span><br><span class="line">    <span class="comment">// ……</span></span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 开发模式中</span></span><br><span class="line">  <span class="keyword">if</span> (__DEV__) &#123;</span><br><span class="line">    <span class="comment">// ……</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="Fiber-工作原理"><a href="#Fiber-工作原理" class="headerlink" title="Fiber 工作原理"></a>Fiber 工作原理</h2><p>Fiber 工作原理最核心的点就是可以中断和恢复, 增强了 react 的并发性和响应性</p>
<p>工作原理中的几个关键点:</p>
<ol>
<li>单元工作: 每个 fiber 节点代表一个单元, 所有 fiber 节点构成一个 fiber 链表树, 使 react 可以细粒度控制节点行为</li>
<li>链接属性: child, sibling, return 分别表示子节点, 兄弟节点和父节点, 构成了 fiber 之间的链接关系, 使 react 能够遍历 fiber 树来确定从哪里开始, 继续或停止工作</li>
<li>双缓冲技术: react 在更新时会根据现有的 fiber 树 (current tree) 创建一个新的临时树 (work in progress (WIP) tree), WIP tree 包含了当前更新受影响的最高节点和其下所有子孙节点, WIP tree 在后台更新, current tree 则是显示在界面上的视图. WIP tree 更新完成后会复制其他节点并最终替换掉 current tree. 因为同时维护两个 fiber tree, 所以 react 可以随时进行比较, 中断或恢复等操作, 这种机制同时保证了渲染性能和 UI 稳定</li>
<li>State &amp; props: memoizedProps, pendingProps, memoizedState 字段让 react 知道组件的上一个状态和即将应用的状态, 通过比较这些值, react 可以决定组件是否需要更新, 避免不必要的渲染</li>
<li>副作用追踪: flags 和 subtreeFlags 标识 fiber 及其子树中需要执行的副作用, 如 DOM 更新, 生命周期调用等, react 会收集这些副作用, 在 commit 阶段一次性执行</li>
</ol>
<h2 id="Fiber-工作流程"><a href="#Fiber-工作流程" class="headerlink" title="Fiber 工作流程"></a>Fiber 工作流程</h2><p>主要分为两个阶段:</p>
<h3 id="Reconciliation-调和"><a href="#Reconciliation-调和" class="headerlink" title="Reconciliation 调和"></a>Reconciliation 调和</h3><p>调和阶段确定了哪些部分的 UI 需要更新, 通过比较新的 props 和旧的 fiber 树来确定, 调和阶段可中断和恢复</p>
<p>调和阶段分为两个小阶段:</p>
<ol>
<li>创建与标记更新节点: beginWork, 这个阶段会进行判断 fiber 是否需要更新, 以及判断 fiber 子节点是更新还是复用, 随后执行执行 fiber 节点的调和(处理诸如新 fiber 的创建, 旧 fiber 的删除或现有 fiber 的更新). beginWork 完成后就会进入 completeWork 流程</li>
<li>收集副作用列表: completeUnitOfWork &amp; completeWork: completeUnitOfWork 负责遍历 fiber 节点, 同时记录有副作用的节点的关系, completeWork 在 completeUnitOfWork 中被调用, 主要用于记录 fiber 的副作用标志, 为子 fiber 创建链表以及根据 fiber 的 tag 进行不同的处理</li>
</ol>
<h3 id="Commit-提交"><a href="#Commit-提交" class="headerlink" title="Commit 提交"></a>Commit 提交</h3><p>提交阶段会通过遍历在 Reconciliation 阶段创建的副作用列表来更新 DOM 并执行收集到的副作用, 提交阶段不可中断</p>
<p>提交阶段分为三个小阶段:</p>
<ol>
<li>遍历副作用列表: commitBeforeMutationEffects, 遍历 fiber, 处理节点删除和确认节点在 before mutation 阶段是否有要处理的副作用</li>
<li>正式提交: commitMutationEffects, 递归遍历 Fiber, 更新副作用节点</li>
<li>处理 layout effects: commitLayoutEffects, 处理那些由 useLayoutEffect 创建的 layout effects</li>
</ol>
]]></content>
      <categories>
        <category>Web</category>
      </categories>
      <tags>
        <tag>React</tag>
        <tag>Fiber</tag>
      </tags>
  </entry>
  <entry>
    <title>用tauri将前端页面打包成桌面应用</title>
    <url>/2022/04/02/Web/tauri-pack-web-app/</url>
    <content><![CDATA[<h2 id="tauri-介绍"><a href="#tauri-介绍" class="headerlink" title="tauri 介绍"></a>tauri 介绍</h2><p>Tauri 是一款使用 rust 开发的应用构建工具包, 让你能够为使用 Web 技术的所有主流桌面操作系统构建软件</p>
<h2 id="安装-tauri"><a href="#安装-tauri" class="headerlink" title="安装 tauri"></a>安装 tauri</h2><h3 id="windows"><a href="#windows" class="headerlink" title="windows"></a>windows</h3><ol>
<li><p>安装 rust</p>
<p>前往<a href="https://www.rust-lang.org/zh-CN/tools/install">https://www.rust-lang.org/zh-CN/tools/install</a>下载并安装 rust 运行环境</p>
</li>
<li><p>在项目中加入 tauri</p>
<p>安装 tauri cli 工具, 并执行创建命令</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pnpm add -D @tauri-apps/cli</span><br><span class="line"></span><br><span class="line">pnpm tauri init</span><br></pre></td></tr></table></figure>

<p>接下来 tauri 会向你提出几个问题, 回答之后会在你的项目根目录加入一个 src-tauri 文件夹, 其中包含了 tauri 的核心运行代码和配置文件, 比较关键的文件如下:</p>
<ul>
<li><p>Cargo.toml</p>
<p>Cargo 清单, 声明了当前应用依赖的 rust 包和元数据等内容</p>
</li>
<li><p>tauri.conf.json</p>
<p>项目构建配置文件, 可高度自定义 tauri 应用的各方面, 可参考<a href="https://tauri.app/zh-cn/v1/api/config">Tauri 的 API 配置</a></p>
</li>
<li><p>src&#x2F;main.rs</p>
<p>rust 程序入口, 其中的 main 函数就是运行时的入口函数</p>
</li>
</ul>
</li>
<li><p>开发环境</p>
<p>在 tauri.conf.json 中可以配置开发环境监听的本地端口和开启开发环境之前需要执行的命令(如一些项目中通常会执行 pnpm dev 来开启本地开发环境)</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;build&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;beforeDevCommand&quot;</span><span class="punctuation">:</span> <span class="string">&quot;pnpm dev&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;devPath&quot;</span><span class="punctuation">:</span> <span class="string">&quot;http://localhost:8888&quot;</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p>执行上述步骤后, 现在你可以执行以下命令来开启开发环境</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pnpm tauri dev</span><br></pre></td></tr></table></figure>

<p>执行完毕后会打开一个窗口, 此时就能看到你的前端界面了</p>
</li>
<li><p>打包应用</p>
<p>打包之前需要先进行一些配置, 比如配置构建应用之前需要执行的打包操作, 以及打包好的文件输出的目录:</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;build&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;beforeBuildCommand&quot;</span><span class="punctuation">:</span> <span class="string">&quot;pnpm build:pack&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;distDir&quot;</span><span class="punctuation">:</span> <span class="string">&quot;../dist&quot;</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p>应用打包后的名称和版本:</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;package&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;productName&quot;</span><span class="punctuation">:</span> <span class="string">&quot;myApp&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;version&quot;</span><span class="punctuation">:</span> <span class="string">&quot;0.1.0&quot;</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p>以及窗口相关的配置, 具体配置可参考<a href="https://tauri.app/zh-cn/v1/api/config">Tauri 的 API 配置</a></p>
<p>打包命令</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pnpm tauri build</span><br></pre></td></tr></table></figure>

<p>打包成功后可以在项目中的 src-tauri&#x2F;target&#x2F;release 目录中找到应用的可执行文件</p>
</li>
<li><p>打包时的错误处理</p>
<p>墙内使用 tauri 打包时可能会遇到一些 github 上的资源下载失败的问题</p>
<ul>
<li><p>Downloading <a href="https://github.com/wixtoolset/wix3/releases/download/wix3112rtm/wix311-binaries.zip">https://github.com/wixtoolset/wix3/releases/download/wix3112rtm/wix311-binaries.zip</a></p>
<p>墙内由于访问 github 非常慢, 下载依赖包大概率失败, 解决办法是直接去下载这个 zip 包, 随后将其中所有文件解压到 C:\Users\用户\AppData\Local\tauri\WixTools 这个目录中, 如果不存在这个目录则手动创建一个</p>
</li>
<li><p>Downloading <a href="https://github.com/tauri-apps/binary-releases/releases/download/nsis-3/nsis-3.zip">https://github.com/tauri-apps/binary-releases/releases/download/nsis-3/nsis-3.zip</a></p>
<p>同理, 这个从 github 上下载的依赖包也大概率失败, 直接下载 zip 包将所有文件解压到 C:\Users\用户\AppData\Local\tauri\NSIS 中</p>
<p>NSIS3 中还有两个依赖的插件会从 github 自动下载, 如果构建应用时下载失败, tauri 会自动删除本地的 NSIS 包</p>
<p>所以我们还需要手动去下载 NSIS3 依赖的两个插件, 其下载地址如下:</p>
<ul>
<li><a href="https://github.com/tauri-apps/binary-releases/releases/download/nsis-plugins-v0/NSIS-ApplicationID.zip">https://github.com/tauri-apps/binary-releases/releases/download/nsis-plugins-v0/NSIS-ApplicationID.zip</a></li>
<li><a href="https://github.com/tauri-apps/nsis-tauri-utils/releases/download/nsis_tauri_utils-v0.1.1/nsis_tauri_utils.dll">https://github.com/tauri-apps/nsis-tauri-utils/releases/download/nsis_tauri_utils-v0.1.1/nsis_tauri_utils.dll</a></li>
</ul>
<p>其中, NSIS-ApplicationID.zip 包将其解压后, 复制目录中的 release\ApplicationID.dll 文件到 C:\Users\用户\AppData\Local\tauri\NSIS\Plugins\x86-unicode 目录中, nsis_tauri_utils.dll 下载完成后同样复制到这个目录中, 随后再次执行 tauri build 就能正常打包出可执行文件了</p>
</li>
</ul>
</li>
</ol>
]]></content>
      <categories>
        <category>Web</category>
      </categories>
      <tags>
        <tag>Rust</tag>
        <tag>Tauri</tag>
        <tag>前端应用</tag>
        <tag>桌面应用</tag>
      </tags>
  </entry>
  <entry>
    <title>Web 端跨域详解</title>
    <url>/2022/04/30/Web/web-cross-domain/</url>
    <content><![CDATA[<h1 id="Web-端跨域详解"><a href="#Web-端跨域详解" class="headerlink" title="Web 端跨域详解"></a>Web 端跨域详解</h1><p>详细介绍 Web 端的跨域问题</p>
<h2 id="跨域三种情况"><a href="#跨域三种情况" class="headerlink" title="跨域三种情况"></a>跨域三种情况</h2><p>当网络请求出现以下情况时, 就属于跨域请求:</p>
<ol>
<li>协议不同, 如 http 和 https</li>
<li>域名不同</li>
<li>端口不同</li>
</ol>
<h2 id="原理"><a href="#原理" class="headerlink" title="原理"></a>原理</h2><p>跨域问题本质上是浏览器行为, 初衷是用于保护用户的访问安全, 属于浏览器同源策略的体现, 这是浏览器最核心和基础的安全功能, 同源策略的限制有以下几种:</p>
<ol>
<li>Cookie、LocalStorage 和 IndexDB 无法读取</li>
<li>DOM 和 JS 对象无法获得</li>
<li>AJAX 请求不能发送</li>
</ol>
<h2 id="解决跨域"><a href="#解决跨域" class="headerlink" title="解决跨域"></a>解决跨域</h2><h3 id="JSONP"><a href="#JSONP" class="headerlink" title="JSONP"></a>JSONP</h3><ul>
<li>原理就是利用 script 标签没有跨域限制, 通过 src 属性设置包含 callback 参数的跨域 GET 请求, 服务端接口将返回数据拼凑到 callback 函数中返回给浏览器, 从而让前端拿到在 callback 函数中获取的数据</li>
</ul>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">script</span>&gt;</span><span class="language-javascript"></span></span><br><span class="line"><span class="language-javascript">  <span class="keyword">var</span> script = <span class="variable language_">document</span>.<span class="title function_">createElement</span>(<span class="string">&quot;script&quot;</span>);</span></span><br><span class="line"><span class="language-javascript">  script.<span class="property">type</span> = <span class="string">&quot;text/javascript&quot;</span>;</span></span><br><span class="line"><span class="language-javascript"></span></span><br><span class="line"><span class="language-javascript">  <span class="comment">// 传参一个回调函数名给后端, 方便后端返回时执行这个在前端定义的回调函数</span></span></span><br><span class="line"><span class="language-javascript">  script.<span class="property">src</span> =</span></span><br><span class="line"><span class="language-javascript">    <span class="string">&quot;http://www.domain2.com:8080/login?user=admin&amp;callback=handleCallback&quot;</span>;</span></span><br><span class="line"><span class="language-javascript">  <span class="variable language_">document</span>.<span class="property">head</span>.<span class="title function_">appendChild</span>(script);</span></span><br><span class="line"><span class="language-javascript"></span></span><br><span class="line"><span class="language-javascript">  <span class="comment">// 回调执行函数</span></span></span><br><span class="line"><span class="language-javascript">  <span class="keyword">function</span> <span class="title function_">handleCallback</span>(<span class="params">res</span>) &#123;</span></span><br><span class="line"><span class="language-javascript">    <span class="title function_">alert</span>(<span class="title class_">JSON</span>.<span class="title function_">stringify</span>(res));</span></span><br><span class="line"><span class="language-javascript">  &#125;</span></span><br><span class="line"><span class="language-javascript"></span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>JSONP 的缺点是只能发送 GET 请求</p>
<h3 id="跨域资源共享-CORS"><a href="#跨域资源共享-CORS" class="headerlink" title="跨域资源共享(CORS)"></a>跨域资源共享(CORS)</h3><p>CORS 是一个 W3C 标准, 它允许浏览器向跨域服务器发送 XHR 请求以客服 AJAX 的同源限制, 需要前后端同时支持</p>
<p>CORS 分为简单请求和非简单请求, 如果请求同时满足以下两个条件则是简单请求:</p>
<ol>
<li>请求方式</li>
</ol>
<ul>
<li><ol>
<li>get</li>
</ol>
</li>
<li><ol start="2">
<li>post</li>
</ol>
</li>
<li><ol start="3">
<li>head</li>
</ol>
</li>
</ul>
<ol start="2">
<li>请求头</li>
</ol>
<ul>
<li><ol>
<li>Accept</li>
</ol>
</li>
<li><ol start="2">
<li>Accept-Language</li>
</ol>
</li>
<li><ol start="3">
<li>Content-Language</li>
</ol>
</li>
<li><ol start="4">
<li>Content-Type 仅限于三个值: application&#x2F;x-www-form-urlencoded, multipart&#x2F;form-data, text&#x2F;plain</li>
</ol>
</li>
</ul>
<p>若没有同时满足以上两个条件就属于非简单请求</p>
<h4 id="简单请求"><a href="#简单请求" class="headerlink" title="简单请求"></a>简单请求</h4><p>发送简单请求时, 浏览器在头信息中增加一个 Origin 字段, 用来说明请求来源(协议 + 域名 + 端口), 服务端根据这个 Origin 来决定是否同意这次请求</p>
<p>CORS 请求设置响应头字段, 都以 Access-Control-开头:</p>
<ol>
<li>Access-Control-Allow-Origin: 必选</li>
</ol>
<ul>
<li>它的值要么是请求时 Origin 字段的值, 要么是一个*, 表示接受任意域名的请求</li>
</ul>
<ol start="2">
<li>Access-Control-Allow-Credentials: 可选</li>
</ol>
<ul>
<li>它的值是一个布尔值, 表示是否允许发送 Cookie. 默认情况下, Cookie 不包括在 CORS 请求之中. 设为 true, 即表示服务器明确许可, Cookie 可以包含在请求中, 一起发给服务器. 这个值也只能设为 true, 如果服务器不要浏览器发送 Cookie, 删除该字段即可</li>
</ul>
<ol start="3">
<li>Access-Control-Expose-Headers: 可选</li>
</ol>
<ul>
<li>CORS 请求时, XMLHttpRequest 对象的 getResponseHeader()方法只能拿到 6 个基本字段: Cache-Control、Content-Language、Content-Type、Expires、Last-Modified、Pragma. 如果想拿到其他字段, 就必须在 Access-Control-Expose-Headers 里面指定. 上面的例子指定, getResponseHeader(‘FooBar’)可以返回 FooBar 字段的值</li>
</ul>
<h4 id="非简单请求"><a href="#非简单请求" class="headerlink" title="非简单请求"></a>非简单请求</h4><p>非简单请求是那种对服务器有特殊要求的请求, 如请求方法是 PUT 或 DELETE, 或者 Content-Type 字段值为 application&#x2F;json 等. 非简单请求会在正是通信前增加一次 http 查询请求, 称为预检请求(preflight)</p>
<p>预检请求的请求方法为 OPTIONS, 表示用于询问, 请求头里除了需要包含 Origin, 还需要包含两个字段:</p>
<ol>
<li>Access-Control-Request-Method: 必选</li>
</ol>
<ul>
<li>用来列出浏览器的 CORS 请求会用到哪些 HTTP 方法, 如 PUT</li>
</ul>
<ol start="2">
<li>Access-Control-Request-Headers: 可选</li>
</ol>
<ul>
<li>该字段是一个逗号分隔的字符串, 指定浏览器 CORS 请求会额外发送的头信息字段, 如 X-Custom-Header</li>
</ul>
<p>服务器收到预检请求后, 检查了这些字段后, 确认允许跨源请求, 就可以做出回应, 响应头中除了 Access-Control-Allow-Origin 外, 还有其他 CORS 相关字段:</p>
<ol>
<li>Access-Control-Allow-Methods: 必选</li>
</ol>
<ul>
<li>它的值是逗号分隔的一个字符串, 表明服务器支持的所有跨域请求的方法。注意, 返回的是所有支持的方法, 而不单是浏览器请求的那个方法, 这是为了避免多次预检请求</li>
</ul>
<ol start="2">
<li>Access-Control-Allow-Headers</li>
</ol>
<ul>
<li>如果浏览器请求包括 Access-Control-Request-Headers 字段, 则 Access-Control-Allow-Headers 字段是必需的, 它也是一个逗号分隔的字符串, 表明服务器支持的所有头信息字段, 不限于浏览器在预检中请求的字段</li>
</ul>
<ol start="3">
<li>Access-Control-Allow-Credentials: 可选</li>
</ol>
<ul>
<li>该字段与简单请求时的含义相同</li>
</ul>
<ol start="4">
<li>Access-Control-Max-Age: 可选</li>
</ol>
<ul>
<li>用来指定本次预检请求的有效期, 单位为秒</li>
</ul>
<h3 id="代理跨域"><a href="#代理跨域" class="headerlink" title="代理跨域"></a>代理跨域</h3><p>通过启动一个代理服务器作为浏览器和服务器之间的中间服务来跨域, 如用 nginx 配置一个与前端同源的代理服务器, 将请求转发给真实的服务器实现反向代理</p>
<h3 id="document-domain-和-iframe"><a href="#document-domain-和-iframe" class="headerlink" title="document.domain 和 iframe"></a>document.domain 和 iframe</h3><p>此方案仅限主域相同, 子域不同的跨域应用场景, 实现原理: 两个页面都通过 js 强制设置 document.domain 为基础主域, 就实现了同域</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!-- 父窗口 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">iframe</span> <span class="attr">id</span>=<span class="string">&quot;iframe&quot;</span> <span class="attr">src</span>=<span class="string">&quot;http://child.domain.com/b.html&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">iframe</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">script</span>&gt;</span><span class="language-javascript"></span></span><br><span class="line"><span class="language-javascript">  <span class="variable language_">document</span>.<span class="property">domain</span> = <span class="string">&quot;domain.com&quot;</span>;</span></span><br><span class="line"><span class="language-javascript">  <span class="keyword">var</span> user = <span class="string">&quot;admin&quot;</span>;</span></span><br><span class="line"><span class="language-javascript"></span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!-- 子窗口 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">script</span>&gt;</span><span class="language-javascript"></span></span><br><span class="line"><span class="language-javascript">  <span class="variable language_">document</span>.<span class="property">domain</span> = <span class="string">&quot;domain.com&quot;</span>;</span></span><br><span class="line"><span class="language-javascript">  <span class="comment">// 获取父窗口中变量</span></span></span><br><span class="line"><span class="language-javascript">  <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&quot;get js data from parent ---&gt; &quot;</span> + <span class="variable language_">window</span>.<span class="property">parent</span>.<span class="property">user</span>);</span></span><br><span class="line"><span class="language-javascript"></span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="postMessage-跨域"><a href="#postMessage-跨域" class="headerlink" title="postMessage 跨域"></a>postMessage 跨域</h3><p>postMessage 是 H5 的 api, 主要用于解决以下问题:</p>
<ul>
<li>页面和其打开的新窗口之间的消息传递</li>
<li>多窗口之间消息传递</li>
<li>页面与嵌套的 iframe 的消息传递</li>
<li>以上场景的跨域消息传递</li>
</ul>
<p>postMessage(data,origin)方法接受两个参数:</p>
<ol>
<li>data: html5 规范支持任意基本类型或可复制的对象, 但部分浏览器只支持字符串, 所以传参时最好用 JSON.stringify()序列化</li>
<li>origin: 协议+主机+端口号, 也可以设置为”*“, 表示可以传递给任意窗口, 如果要指定和当前窗口同源的话设置为”&#x2F;“</li>
</ol>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!-- a.html --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">iframe</span></span></span><br><span class="line"><span class="tag">  <span class="attr">id</span>=<span class="string">&quot;iframe&quot;</span></span></span><br><span class="line"><span class="tag">  <span class="attr">src</span>=<span class="string">&quot;http://www.domain2.com/b.html&quot;</span></span></span><br><span class="line"><span class="tag">  <span class="attr">style</span>=<span class="string">&quot;display:none;&quot;</span></span></span><br><span class="line"><span class="tag">&gt;</span><span class="tag">&lt;/<span class="name">iframe</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">script</span>&gt;</span><span class="language-javascript"></span></span><br><span class="line"><span class="language-javascript">  <span class="keyword">var</span> iframe = <span class="variable language_">document</span>.<span class="title function_">getElementById</span>(<span class="string">&quot;iframe&quot;</span>);</span></span><br><span class="line"><span class="language-javascript">  iframe.<span class="property">onload</span> = <span class="keyword">function</span> (<span class="params"></span>) &#123;</span></span><br><span class="line"><span class="language-javascript">    <span class="keyword">var</span> data = &#123;</span></span><br><span class="line"><span class="language-javascript">      <span class="attr">name</span>: <span class="string">&quot;aym&quot;</span>,</span></span><br><span class="line"><span class="language-javascript">    &#125;;</span></span><br><span class="line"><span class="language-javascript">    <span class="comment">// 向domain2传送跨域数据</span></span></span><br><span class="line"><span class="language-javascript">    iframe.<span class="property">contentWindow</span>.<span class="title function_">postMessage</span>(</span></span><br><span class="line"><span class="language-javascript">      <span class="title class_">JSON</span>.<span class="title function_">stringify</span>(data),</span></span><br><span class="line"><span class="language-javascript">      <span class="string">&quot;http://www.domain2.com&quot;</span></span></span><br><span class="line"><span class="language-javascript">    );</span></span><br><span class="line"><span class="language-javascript">  &#125;;</span></span><br><span class="line"><span class="language-javascript"></span></span><br><span class="line"><span class="language-javascript">  <span class="comment">// 接受domain2返回数据</span></span></span><br><span class="line"><span class="language-javascript">  <span class="variable language_">window</span>.<span class="title function_">addEventListener</span>(</span></span><br><span class="line"><span class="language-javascript">    <span class="string">&quot;message&quot;</span>,</span></span><br><span class="line"><span class="language-javascript">    <span class="keyword">function</span> (<span class="params">e</span>) &#123;</span></span><br><span class="line"><span class="language-javascript">      <span class="title function_">alert</span>(<span class="string">&quot;data from domain2 ---&gt; &quot;</span> + e.<span class="property">data</span>);</span></span><br><span class="line"><span class="language-javascript">    &#125;,</span></span><br><span class="line"><span class="language-javascript">    <span class="literal">false</span></span></span><br><span class="line"><span class="language-javascript">  );</span></span><br><span class="line"><span class="language-javascript"></span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!-- b.html --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">script</span>&gt;</span><span class="language-javascript"></span></span><br><span class="line"><span class="language-javascript">  <span class="comment">// 接收domain1的数据</span></span></span><br><span class="line"><span class="language-javascript">  <span class="variable language_">window</span>.<span class="title function_">addEventListener</span>(</span></span><br><span class="line"><span class="language-javascript">    <span class="string">&quot;message&quot;</span>,</span></span><br><span class="line"><span class="language-javascript">    <span class="keyword">function</span> (<span class="params">e</span>) &#123;</span></span><br><span class="line"><span class="language-javascript">      <span class="title function_">alert</span>(<span class="string">&quot;data from domain1 ---&gt; &quot;</span> + e.<span class="property">data</span>);</span></span><br><span class="line"><span class="language-javascript"></span></span><br><span class="line"><span class="language-javascript">      <span class="keyword">var</span> data = <span class="title class_">JSON</span>.<span class="title function_">parse</span>(e.<span class="property">data</span>);</span></span><br><span class="line"><span class="language-javascript">      <span class="keyword">if</span> (data) &#123;</span></span><br><span class="line"><span class="language-javascript">        data.<span class="property">number</span> = <span class="number">16</span>;</span></span><br><span class="line"><span class="language-javascript"></span></span><br><span class="line"><span class="language-javascript">        <span class="comment">// 处理后再发回domain1</span></span></span><br><span class="line"><span class="language-javascript">        <span class="variable language_">window</span>.<span class="property">parent</span>.<span class="title function_">postMessage</span>(</span></span><br><span class="line"><span class="language-javascript">          <span class="title class_">JSON</span>.<span class="title function_">stringify</span>(data),</span></span><br><span class="line"><span class="language-javascript">          <span class="string">&quot;http://www.domain1.com&quot;</span></span></span><br><span class="line"><span class="language-javascript">        );</span></span><br><span class="line"><span class="language-javascript">      &#125;</span></span><br><span class="line"><span class="language-javascript">    &#125;,</span></span><br><span class="line"><span class="language-javascript">    <span class="literal">false</span></span></span><br><span class="line"><span class="language-javascript">  );</span></span><br><span class="line"><span class="language-javascript"></span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="WebSocket-跨域"><a href="#WebSocket-跨域" class="headerlink" title="WebSocket 跨域"></a>WebSocket 跨域</h3><p>WebSocket protocol 是 HTML5 的一种协议, 它实现了浏览器与服务器全双工通信, 同时允许跨域通讯, 是 server push 技术的一种很好的实现</p>
]]></content>
      <categories>
        <category>Web</category>
      </categories>
      <tags>
        <tag>跨域</tag>
        <tag>http</tag>
        <tag>https</tag>
        <tag>域名</tag>
        <tag>端口</tag>
      </tags>
  </entry>
  <entry>
    <title>Vue3 细节整理</title>
    <url>/2022/05/02/Web/vue3-details/</url>
    <content><![CDATA[<h1 id="Vue3-细节整理"><a href="#Vue3-细节整理" class="headerlink" title="Vue3 细节整理"></a>Vue3 细节整理</h1><p>实际使用中遇到的一些 Vue3 操作的细节和问题的记录</p>
<h2 id="reactive-的局限"><a href="#reactive-的局限" class="headerlink" title="reactive 的局限"></a>reactive 的局限</h2><ol>
<li>只能用于对象类型(object, array, Map, Set 等), 不能用于原始值类型 (boolean, number, string 等), 因为 Proxy 只能作用于对象结构</li>
<li>不能替换整个对象, 必须始终保持响应式对象的相同引用</li>
<li>结构后对象属性会失去响应式特性</li>
</ol>
<h2 id="ref-特性"><a href="#ref-特性" class="headerlink" title="ref 特性"></a>ref 特性</h2><ol>
<li>ref 会将任意类型包装成一个 Proxy 对象, 通过内部的 value 属性挂载实际值</li>
<li>如果传递原始数据类型, 则 ref 本身是一个 RefImpl 对象, 而 value 是原始值, 如果传递引用数据类型, 则 value 会包装成 Proxy 对象</li>
<li>ref 变量作为顶层属性时可以自动解包, 如 const count &#x3D; ref(0), 在模板中应用时 <span>{count}<span/>, 而引用数据类型则不能自动解包, 如 const person &#x3D; ref({ age: 2 }), 在模板中 <span>{person.age}<span/> 无法正确获取到值, 需要手动解包, 如 const { age } &#x3D; person</li>
</ol>
<h2 id="父子组件执行顺序"><a href="#父子组件执行顺序" class="headerlink" title="父子组件执行顺序"></a>父子组件执行顺序</h2><p>父 setup -》 父 onBeforeMount &#x3D;〉子 setup -》子 onBeforeMount -〉子 onMounted -》 父 onMounted</p>
<h2 id="watch-深层监听"><a href="#watch-深层监听" class="headerlink" title="watch 深层监听"></a>watch 深层监听</h2><ol>
<li>当传递一个响应式对象给 watch 而不是一个 getter 函数时, watch 会自动创建响应式对象的深层监听, 如:</li>
</ol>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> obj = <span class="title function_">reactive</span>(&#123;</span><br><span class="line">  <span class="attr">inner</span>: &#123;</span><br><span class="line">    <span class="attr">count</span>: <span class="number">10</span>,</span><br><span class="line">  &#125;,</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> <span class="title function_">addObjCount</span> = (<span class="params"></span>) =&gt; &#123;</span><br><span class="line">  obj.<span class="property">inner</span>.<span class="property">count</span>++;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 此时修改 count 时会触发 watch 回调函数</span></span><br><span class="line"><span class="title function_">watch</span>(obj, <span class="function">(<span class="params">newValue, oldValue</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&quot;新的 obj:&quot;</span>, newValue.<span class="property">inner</span>);</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&quot;旧的 obj:&quot;</span>, oldValue.<span class="property">inner</span>);</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 等价于</span></span><br><span class="line"><span class="comment">// watch(</span></span><br><span class="line"><span class="comment">//   () =&gt; obj,</span></span><br><span class="line"><span class="comment">//   (newValue, oldValue) =&gt; &#123;</span></span><br><span class="line"><span class="comment">//     console.log(&quot;新的 obj:&quot;, newValue.inner);</span></span><br><span class="line"><span class="comment">//     console.log(&quot;旧的 obj:&quot;, oldValue.inner);</span></span><br><span class="line"><span class="comment">//   &#125;,</span></span><br><span class="line"><span class="comment">//   &#123;</span></span><br><span class="line"><span class="comment">//     deep: true,</span></span><br><span class="line"><span class="comment">//   &#125;</span></span><br><span class="line"><span class="comment">// );</span></span><br></pre></td></tr></table></figure>

<p>此时绑定 addObjCount 函数到点击事件, 会发现每次点击都触发了 watch 回调函数执行, 而修改 watch 的参数为 getter 函数后, 则不会触发</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 此时修改 count 时不会触发 watch 回调函数</span></span><br><span class="line"><span class="title function_">watch</span>(</span><br><span class="line">  <span class="function">() =&gt;</span> obj,</span><br><span class="line">  <span class="function">(<span class="params">newValue, oldValue</span>) =&gt;</span> &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&quot;新的 obj:&quot;</span>, newValue.<span class="property">inner</span>);</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&quot;旧的 obj:&quot;</span>, oldValue.<span class="property">inner</span>);</span><br><span class="line">  &#125;</span><br><span class="line">);</span><br></pre></td></tr></table></figure>

<h2 id="watch-和-watchEffect-区别"><a href="#watch-和-watchEffect-区别" class="headerlink" title="watch 和 watchEffect 区别"></a>watch 和 watchEffect 区别</h2><ol>
<li>watch 需要指明监听对象, watchEffect 隐式监听回调函数中的响应式数据</li>
<li>watch 只有在数据变化时执行回调函数, watchEffect 会在响应式数据初始化时就执行回调函数</li>
</ol>
<h2 id="watch-watchEffect-回调函数执行时机"><a href="#watch-watchEffect-回调函数执行时机" class="headerlink" title="watch &#x2F; watchEffect 回调函数执行时机"></a>watch &#x2F; watchEffect 回调函数执行时机</h2><p>默认情况下, 监听回调执行时机是在 vue 组件渲染到 dom 之前, 如果设置了 flush: ‘post’ 则是在渲染之后执行, 可以获取到更新后的 dom</p>
<h2 id="v-show-和-v-if-异同"><a href="#v-show-和-v-if-异同" class="headerlink" title="v-show 和 v-if 异同"></a>v-show 和 v-if 异同</h2><p>共同点:</p>
<ol>
<li>控制元素是否显示在界面</li>
<li>为 true 时会占据页面位置, 为 false 时不会占据</li>
<li>都会导致回流(reflow)和重绘(repaint)</li>
</ol>
<p>不同点:</p>
<ol>
<li>v-show 无论何值都会渲染元素, 但使用 css display 属性来控制显示隐藏, v-if 为 false 则是直接不渲染元素</li>
<li>v-show 由 false 变为 true 不会触发生命周期, v-if 由 false 变为 true 会触发 create 和 mount 阶段的生命周期钩子, true 改为 false 则会触发 destroy 阶段的钩子</li>
<li>v-if 有更高的切换性能消耗, v-show 则是更高的初始渲染消耗</li>
</ol>
<h2 id="v-for-中的-key"><a href="#v-for-中的-key" class="headerlink" title="v-for 中的 key"></a>v-for 中的 key</h2><p>Vue 在处理更新同类型 vnode 的一组子节点的过程中, 为了减少 DOM 频繁创建和销毁的性能开销</p>
<p>假设要更新这样一个节点列表</p>
<p><img src="/images/vue3-details-01.png" alt="更新vue节点"></p>
<ol>
<li>对没有 key 的子节点数组更新调用的是 patchUnkeyedChildren 这个方法, 核心是就地更新的策略. 它会通过对比新旧子节点数组的长度, 先以比较短的那部分长度为基准, 将新子节点的那一部分直接 patch 上去. 然后再判断, 如果是新子节点数组的长度更长, 就直接将新子节点数组剩余部分挂载(mount); 如果是新子节点数组更短, 就把旧子节点多出来的那部分给卸载掉(unmount). 所以如果子节点是组件或者有状态的 DOM 元素, 原有的状态会保留, 就会出现渲染不正确的问题</li>
</ol>
<p><img src="/images/vue3-details-02.png" alt="更新不带key的节点"></p>
<ol start="2">
<li>有 key 的子节点更新是调用的 patchKeyedChildren, 这个函数就是大家熟悉的实现核心 diff 算法的地方, 大概流程就是同步头部节点, 同步尾部节点, 处理新增和删除的节点, 最后用求解最长递增子序列的方法去处理未知子序列. 是为了最大程度实现对已有节点的复用, 减少 DOM 操作的性能开销, 同时避免了就地更新带来的子节点状态错误的问题</li>
</ol>
<p><img src="/images/vue3-details-03.png" alt="更新带key的节点"></p>
<p>如果是用 v-for 去遍历常量或者子节点是诸如纯文本这类没有状态的节点, 是可以使用不加 key 的写法的。但是实际开发过程中更推荐统一加上 key, 能够实现更广泛场景的同时, 避免了可能发生的状态更新错误, 我们一般可以使用 ESlint 配置 key 为 v-for 的必需元素</p>
<p>通常在数据量大的列表等情况下推荐给元素加上 key, 可以有效减少 diff 的开销, 因为没有 key, 算法需要进入子节点深入比较变化, 加上 key 可以最大程度复用节点, 进行节点位置的移动而不是频繁销毁和创建新的节点</p>
<h2 id="vue3-中的宏命令"><a href="#vue3-中的宏命令" class="headerlink" title="vue3 中的宏命令"></a>vue3 中的宏命令</h2><p>宏是一种特殊代码, 运行在编译阶段, 会被编译器转换成其他代码, 实际上是一种巧妙地字符串替换技巧, 根据功能不同, 转换后的代码也不同</p>
<h3 id="为什么宏不用-import"><a href="#为什么宏不用-import" class="headerlink" title="为什么宏不用 import"></a>为什么宏不用 import</h3><p>import 的模块在运行时执行, 编译时执行的宏命令不需要 import</p>
<h3 id="注意点"><a href="#注意点" class="headerlink" title="注意点"></a>注意点</h3><p>编译时只会处理 setup 顶层的宏, 被包裹在代码块中的宏不会被编译器转换, 导致在运行时执行这些代码时报错</p>
<h2 id="nextTick"><a href="#nextTick" class="headerlink" title="nextTick"></a>nextTick</h2><p>在下次 DOM 更新循环结束之后执行延迟回调。在修改数据之后立即使用这个方法, 获取更新后的 DOM</p>
<h3 id="用法"><a href="#用法" class="headerlink" title="用法"></a>用法</h3><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; createApp, nextTick &#125; <span class="keyword">from</span> <span class="string">&quot;vue&quot;</span>;</span><br><span class="line"><span class="keyword">const</span> app = <span class="title function_">createApp</span>(&#123;</span><br><span class="line">  <span class="title function_">setup</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="keyword">const</span> message = <span class="title function_">ref</span>(<span class="string">&quot;Hello!&quot;</span>);</span><br><span class="line">    <span class="keyword">const</span> <span class="title function_">changeMessage</span> = <span class="keyword">async</span> (<span class="params">newMessage</span>) =&gt; &#123;</span><br><span class="line">      message.<span class="property">value</span> = newMessage;</span><br><span class="line">      <span class="comment">// 这里获取DOM的value是旧值</span></span><br><span class="line">      <span class="keyword">await</span> <span class="title function_">nextTick</span>();</span><br><span class="line">      <span class="comment">// nextTick 后获取DOM的value是更新后的值</span></span><br><span class="line">      <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&quot;Now DOM is updated&quot;</span>);</span><br><span class="line">    &#125;;</span><br><span class="line">  &#125;,</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<h3 id="特性"><a href="#特性" class="headerlink" title="特性"></a>特性</h3><ol>
<li>如果你想捕捉组件数据变化后 DOM 更新的时刻, 那么你需要使用 nextTick(callback) 函数</li>
<li>它们的单个 callback 参数在 DOM 更新后立即被调用: 并且你可以保证获得与组件数据同步的最新 DOM</li>
<li>或者, 如果你不向 nextTick() 提供回调参数, 那么这些函数将返回一个在 DOM 更新时被解析的 promise</li>
</ol>
<h2 id="h-函数"><a href="#h-函数" class="headerlink" title="h 函数"></a>h 函数</h2><p>返回一个”虚拟节点”, 通常缩写为 VNode: 一个普通对象, 其中包含向 Vue 描述它应该在页面上呈现哪种节点的信息, 包括对任何子节点的描述, 用于手动编写 render</p>
<p>h 接受三个参数</p>
<ol>
<li>type 元素类型</li>
<li>propsOrChildren 数据对象, 如 props, domProps, class, attrs, style 等</li>
<li>children 子节点</li>
</ol>
<p>通过内部的 _createVNode 方法生成 VNode 虚拟节点</p>
]]></content>
      <categories>
        <category>Web</category>
      </categories>
      <tags>
        <tag>Vue3</tag>
        <tag>Vue3 细节</tag>
      </tags>
  </entry>
</search>
